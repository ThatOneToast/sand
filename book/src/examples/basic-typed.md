# Basic Typed Datapack

See `examples/basic_typed.rs` for a compact load/tick/function example.

It demonstrates:

- `#[component(Load)]`
- `#[component(Tick)]`
- `#[function]`
- typed score state
- typed text and actionbar commands
