# Release

Before release, verify:

- workspace formatting, clippy, tests, macro trybuild tests, and rustdoc
- mdBook builds with `mdbook build`
- examples still teach attribute-first typed authoring
- escape hatches are explicit and documented
- version support notes match the generated Minecraft data
