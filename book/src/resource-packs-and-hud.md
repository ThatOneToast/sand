# Resource Packs And HUD

Sand includes optional resource-pack and HUD helpers. These APIs are still more
experimental than typed datapack authoring.

Use resource-pack output when your datapack needs generated textures, font
glyphs, HUD bars, or custom item models. Keep datapack logic in typed attribute
functions and treat resource-pack generation as a companion output.
