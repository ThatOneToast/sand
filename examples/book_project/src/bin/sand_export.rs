//! Generated by `sand` — do not edit by hand.
//!
//! Invoked by `sand build` to collect all registered datapack components and
//! print them as JSON. `#[function]` and `#[component]` items are discovered
//! automatically via inventory — no manual registration needed.

fn main() {
    let mc_version = std::env::var("SAND_EXPORT_MC_VERSION").unwrap_or_else(|_| {
        eprintln!(
            "sand export failed: SAND_EXPORT_MC_VERSION is missing; invoke this binary through `sand build`"
        );
        std::process::exit(1);
    });
    book_project::__sand_export("trail", &mc_version);
}
