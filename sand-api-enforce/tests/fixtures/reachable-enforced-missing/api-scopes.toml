schema_version = 1
static_surface_items = 12
enforced_scope_baseline = ["predicate-source", "execute-when-source", "condition-source"]
pending_scope_ceiling = 0
pending_item_ceiling = 0

[[scope]]
id = "predicate-source"
canonical_module = "sand::predicate"
state = "enforced"
tier = "author"
provider = "source"
recursive = true

[[scope]]
id = "execute-when-source"
canonical_module = "sand::execute_when"
state = "enforced"
tier = "author"
provider = "source"
recursive = true

[[scope]]
id = "condition-source"
canonical_module = "sand::condition"
state = "enforced"
tier = "author"
provider = "source"
recursive = true
