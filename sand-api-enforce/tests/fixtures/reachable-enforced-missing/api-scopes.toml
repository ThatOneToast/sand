schema_version = 1
static_surface_items = 25
enforced_scope_baseline = ["predicate-source", "execute-when-source", "condition-source", "resource-ref-source", "version-source", "vfx-source", "advanced-source", "root-source"]
pending_scope_ceiling = 0
pending_item_ceiling = 0

[[scope]]
id = "root-source"
canonical_module = "sand"
state = "enforced"
tier = "author"
provider = "source"
recursive = false

[[scope]]
id = "predicate-source"
canonical_module = "sand::predicate"
state = "enforced"
tier = "author"
provider = "source"
recursive = true

[[scope]]
id = "execute-when-source"
canonical_module = "sand::execute_when"
state = "enforced"
tier = "author"
provider = "source"
recursive = true

[[scope]]
id = "condition-source"
canonical_module = "sand::condition"
state = "enforced"
tier = "author"
provider = "source"
recursive = true

[[scope]]
id = "resource-ref-source"
canonical_module = "sand::resource_ref"
state = "enforced"
tier = "author"
provider = "source"
recursive = true

[[scope]]
id = "version-source"
canonical_module = "sand::version"
state = "enforced"
tier = "author"
provider = "source"
recursive = true

[[scope]]
id = "vfx-source"
canonical_module = "sand::vfx"
state = "enforced"
tier = "author"
provider = "source"
recursive = true

[[scope]]
id = "advanced-source"
canonical_module = "sand::advanced"
state = "enforced"
tier = "advanced"
provider = "source"
recursive = true
