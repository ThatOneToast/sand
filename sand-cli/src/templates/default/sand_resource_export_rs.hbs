//! Generated by `sand` — edit to match your crate name.
//!
//! Invoked by `sand build --resourcepack` to collect all registered resource
//! pack components and print them as JSON. Components registered via
//! `hud_bar!`, `hud_element!`, and `texture!` are discovered automatically
//! via inventory — no manual wiring needed.

fn main() {
    {{name_snake}}::__sand_resource_export("{{namespace}}");
}
