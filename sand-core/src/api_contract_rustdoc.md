# API Contract

This item is part of Sand's supported author API. In the `sand` facade docs,
copy the canonical `sand::...` path shown at the top of the page and run
`sand api show <canonical-path>` for the validated contract, exact signature,
Minecraft behavior, availability, and aliases. Append an associated item's
name to the displayed owner path when inspecting a method, field, or variant.
