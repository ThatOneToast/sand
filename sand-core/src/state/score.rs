//! Typed scoreboard variable — wraps a scoreboard objective for clean access.
//!
//! # API hierarchy (see [#146](https://github.com/ThatOneToast/sand/issues/146))
//!
//! 1. **Typed normal API** — `try_*` methods (e.g. [`ScoreVar::try_set`],
//!    [`ScoreVar::try_of`]) take a typed [`sand_commands::ScoreHolder`]
//!    (an entity selector, `@s`, a literal player name, or a `#`-prefixed
//!    fake player) and validate it before generating any command text.
//!    Prefer these in new code.
//! 2. **Validated compatibility adapter** — [`sand_commands::scoreboard::Objective`]
//!    offers the same validated surface directly against
//!    [`sand_commands::ObjectiveName`]/[`sand_commands::ScoreHolder`] for
//!    callers that don't need `ScoreVar`'s condition-builder ergonomics.
//! 3. **Raw escape hatch** — the plain (infallible) methods (e.g.
//!    [`ScoreVar::set`], [`ScoreVar::of`]) accept any `impl Display`/`&str`
//!    and interpolate it into command text without validation. They remain
//!    available for compatibility and for advanced/modded selector syntax
//!    Sand cannot yet type-check.

use std::collections::BTreeMap;
use std::marker::PhantomData;
use std::ops::RangeBounds;
use std::sync::{Mutex, OnceLock};

#[cfg(test)]
use crate::condition::ConditionKind;
use crate::condition::{Condition, ScoreCompareOp, ScoreRange};
use crate::execute_when::Conditional;

#[sand_macros::api(
    registry = sand_api_contract,
    path = "sand::state::ScoreOperand",
    module = "sand::state",
    summary = "One owned scoreboard entry used by score operations and comparisons.",
    context = "One owned scoreboard entry used by score operations and comparisons. Callers normally obtain an operand from [`ScoreRef::operand`] or [`ScoreConst::ref_`] rather than constructing one directly.",
    minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
    use_when = ["Working with typed gameplay state or composing state transitions"],
    avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
    example = "use sand::state::ScoreOperand;",
)]
/// One owned scoreboard entry used by score operations and comparisons.
///
/// Callers normally obtain an operand from [`ScoreRef::operand`] or
/// [`ScoreConst::ref_`] rather than constructing one directly.
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct ScoreOperand {
    pub(crate) selector: String,
    pub(crate) objective: String,
}
use crate::state::storage::StorageField;

// ── Name utilities ────────────────────────────────────────────────────────────

/// Minecraft scoreboard objective names are limited to 16 characters.
///
/// Delegates to [`sand_commands::scoreboard::ObjectiveName::logical`] so
/// `ScoreVar`'s generated objective names use the exact same deterministic
/// hashing algorithm as `sand_commands::scoreboard::Objective` (see
/// [#146](https://github.com/ThatOneToast/sand/issues/146) — "do not leave
/// `sand-commands` and `sand-core` with unrelated validation and hashing
/// behavior"). If `name` fits and is already a valid direct objective token,
/// it is used verbatim; otherwise (too long, or containing characters an
/// objective name cannot use, such as whitespace) it is hashed to a stable,
/// always-valid ≤16-character name prefixed with `"s"`.
pub(super) fn objective_name(name: &str) -> String {
    sand_commands::ObjectiveName::logical(name)
        .as_str()
        .to_string()
}

fn fnv1a(s: &str) -> u64 {
    let mut h: u64 = 14_695_981_039_346_656_037;
    for b in s.bytes() {
        h ^= b as u64;
        h = h.wrapping_mul(1_099_511_628_211);
    }
    h
}

#[sand_macros::api(
    registry = sand_api_contract,
    path = "sand::state::ScoreOperation",
    module = "sand::state",
    summary = "Vanilla scoreboard-player operations.",
    context = "Vanilla scoreboard-player operations. This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
    minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
    use_when = ["Working with typed gameplay state or composing state transitions"],
    avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
    example = "use sand::state::ScoreOperation;",
    variants(Add = "Selects the add operation or policy for typed gameplay state.", Assign = "Selects the assign operation or policy for typed gameplay state.", Div = "Selects the div operation or policy for typed gameplay state.", Max = "Selects the max operation or policy for typed gameplay state.", Min = "Selects the min operation or policy for typed gameplay state.", Mod = "Selects the mod operation or policy for typed gameplay state.", Mul = "Selects the mul operation or policy for typed gameplay state.", Sub = "Selects the sub operation or policy for typed gameplay state.", Swap = "Selects the swap operation or policy for typed gameplay state."),
)]
/// Vanilla scoreboard-player operations.
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum ScoreOperation {
    #[doc = "Selects the assign operation or policy for typed gameplay state."]
    Assign,
    #[doc = "Selects the add operation or policy for typed gameplay state."]
    Add,
    #[doc = "Selects the sub operation or policy for typed gameplay state."]
    Sub,
    #[doc = "Selects the mul operation or policy for typed gameplay state."]
    Mul,
    #[doc = "Selects the div operation or policy for typed gameplay state."]
    Div,
    #[doc = "Selects the mod operation or policy for typed gameplay state."]
    Mod,
    #[doc = "Selects the min operation or policy for typed gameplay state."]
    Min,
    #[doc = "Selects the max operation or policy for typed gameplay state."]
    Max,
    #[doc = "Selects the swap operation or policy for typed gameplay state."]
    Swap,
}

impl ScoreOperation {
    /// Render this operation as vanilla command syntax.
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreOperation::as_str",
        module = "sand::state",
        kind = "method",
        summary = "Render this operation as vanilla command syntax.",
        context = "Render this operation as vanilla command syntax. This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        returns = "The rendered Minecraft command text produced to render this operation as vanilla command syntax.",
        example = "use sand::prelude::*;\n\nfn demonstrate(score_operation_value: sand::state::ScoreOperation)  {\n    let as_str = score_operation_value.as_str();\n}",
    )]
    pub const fn as_str(self) -> &'static str {
        match self {
            Self::Assign => "=",
            Self::Add => "+=",
            Self::Sub => "-=",
            Self::Mul => "*=",
            Self::Div => "/=",
            Self::Mod => "%=",
            Self::Min => "<",
            Self::Max => ">",
            Self::Swap => "><",
        }
    }
}

#[sand_macros::api(
    registry = sand_api_contract,
    path = "sand::state::ScoreConstants",
    module = "sand::state",
    summary = "A namespace for reusable fake-player score constants.",
    context = "A namespace for reusable fake-player score constants. This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
    minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
    use_when = ["Working with typed gameplay state or composing state transitions"],
    avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
    example = "use sand::state::ScoreConstants;",
)]
/// A namespace for reusable fake-player score constants.
#[derive(Debug, Clone, Copy)]
pub struct ScoreConstants {
    objective: &'static str,
}

impl ScoreConstants {
    /// Create a constant namespace. Its objective is created automatically in
    /// Sand's generated load function when one of its constants is used.
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreConstants::new",
        module = "sand::state",
        kind = "method",
        summary = "Create a constant namespace. Its objective is created automatically in Sand's generated load function when one of its constants is used.",
        context = "Create a constant namespace. Its objective is created automatically in Sand's generated load function when one of its constants is used. This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(objective = "`objective` supplies the objective value used to create a constant namespace. Its objective is created automatically in Sand's generated load function when one of its constants is used."),
        returns = "A newly constructed `ScoreConstants` configured to create a constant namespace. Its objective is created automatically in Sand's generated load function when one of its constants is used.",
        example = "use sand::prelude::*;\n\nfn demonstrate(objective: & 'static str)  {\n    let score_constants = sand::state::ScoreConstants::new(objective);\n}",
    )]
    pub const fn new(objective: &'static str) -> Self {
        Self { objective }
    }

    /// Define a typed integer constant.
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreConstants::i32",
        module = "sand::state",
        kind = "method",
        summary = "Define a typed integer constant.",
        context = "Define a typed integer constant. This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(name = "`name` provides the author-visible text value used to define a typed integer constant.", value = "`value` provides the value being applied or compared used to define a typed integer constant."),
        returns = "The `ScoreConst < i32 >` value produced to define a typed integer constant.",
        example = "use sand::prelude::*;\n\nfn demonstrate(score_constants_value: &sand::state::ScoreConstants, name: & 'static str, value: i32)  {\n    let i32 = score_constants_value.i32(name, value);\n}",
    )]
    pub const fn i32(&self, name: &'static str, value: i32) -> ScoreConst<i32> {
        ScoreConst {
            objective: self.objective,
            name,
            value,
            _marker: PhantomData,
        }
    }
}

#[sand_macros::api(
    registry = sand_api_contract,
    path = "sand::state::ScoreConst",
    module = "sand::state",
    summary = "A typed fake-player constant for scoreboard operations and comparisons.",
    context = "A typed fake-player constant for scoreboard operations and comparisons. This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
    minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
    use_when = ["Working with typed gameplay state or composing state transitions"],
    avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
    example = "use sand::state::ScoreConst;",
)]
/// A typed fake-player constant for scoreboard operations and comparisons.
#[derive(Debug, Clone, Copy)]
pub struct ScoreConst<T = i32> {
    objective: &'static str,
    name: &'static str,
    value: i32,
    _marker: PhantomData<T>,
}

impl<T> ScoreConst<T> {
    /// Construct a constant in Sand's default `sand_consts` objective.
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreConst::new",
        module = "sand::state",
        kind = "method",
        summary = "Construct a constant in Sand's default `sand_consts` objective.",
        context = "Construct a constant in Sand's default `sand_consts` objective. This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(name = "`name` provides the author-visible text value used to construct a constant in Sand's default `sand_consts` objective.", value = "`value` provides the value being applied or compared used to construct a constant in Sand's default `sand_consts` objective."),
        returns = "A newly constructed `ScoreConst` configured to construct a constant in Sand's default `sand_consts` objective.",
        example = "use sand::prelude::*;\n\nfn demonstrate<T: 'static>(name: & 'static str, value: i32)  {\n    let score_const = sand::state::ScoreConst ::< T >::new(name, value);\n}",
    )]
    pub const fn new(name: &'static str, value: i32) -> Self {
        Self {
            objective: "sand_consts",
            name,
            value,
            _marker: PhantomData,
        }
    }

    /// Return this constant as a score operand and register its deterministic
    /// load-time setup. Reusing the same name/value is deduplicated; a
    /// conflicting definition panics during pack generation rather than
    /// silently changing scoreboard math.
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreConst::ref_",
        module = "sand::state",
        kind = "method",
        summary = "Return this constant as a score operand and register its deterministic load-time setup. Reusing the same name/value is deduplicated; a conflicting definition panics during pack generation rather than silently changing scoreboard math.",
        context = "Return this constant as a score operand and register its deterministic load-time setup. Reusing the same name/value is deduplicated; a conflicting definition panics during pack generation rather than silently changing scoreboard math. This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        returns = "Return this constant as a score operand and register its deterministic load-time setup. Reusing the same name/value is deduplicated; a conflicting definition panics during pack generation rather than silently changing scoreboard math.",
        example = "use sand::prelude::*;\n\nfn demonstrate<T: 'static>(score_const_value: sand::state::ScoreConst < T >)  {\n    let ref_ = score_const_value.ref_();\n}",
    )]
    pub fn ref_(self) -> ScoreOperand {
        let objective = objective_name(self.objective);
        let holder = constant_holder(self.name);
        register_constant(&objective, &holder, self.value);
        ScoreOperand {
            selector: holder,
            objective,
        }
    }
}

fn constant_holder(name: &str) -> String {
    let clean: String = name
        .chars()
        .filter(|c| c.is_ascii_alphanumeric() || matches!(c, '_' | '-' | '.'))
        .collect();
    let clean = if clean.is_empty() { "const" } else { &clean };
    let prefix: String = clean.chars().take(28).collect();
    format!("#sand_{prefix}_{:06x}", fnv1a(name) & 0xFF_FFFF)
}

fn constants_registry() -> &'static Mutex<BTreeMap<(String, String), i32>> {
    static REGISTRY: OnceLock<Mutex<BTreeMap<(String, String), i32>>> = OnceLock::new();
    REGISTRY.get_or_init(|| Mutex::new(BTreeMap::new()))
}

fn register_constant(objective: &str, holder: &str, value: i32) {
    let mut registry = constants_registry()
        .lock()
        .expect("score constant registry poisoned");
    let key = (objective.to_string(), holder.to_string());
    if let Some(existing) = registry.insert(key.clone(), value)
        && existing != value
    {
        panic!(
            "conflicting Sand score constant `{}` in objective `{}`: {existing} versus {value}",
            holder, objective
        );
    }
}

fn score_constant_operand(prefix: &str, value: i32) -> ScoreOperand {
    let objective = objective_name("sand_consts");
    let holder = constant_holder(&format!("{prefix}_{value}"));
    register_constant(&objective, &holder, value);
    ScoreOperand {
        selector: holder,
        objective,
    }
}

fn score_set_command(target: &ScoreOperand, value: i32) -> String {
    format!(
        "scoreboard players set {} {} {value}",
        target.selector, target.objective
    )
}

fn score_operation_command(
    target: &ScoreOperand,
    op: ScoreOperation,
    source: &ScoreOperand,
) -> String {
    format!(
        "scoreboard players operation {} {} {} {} {}",
        target.selector,
        target.objective,
        op.as_str(),
        source.selector,
        source.objective
    )
}

/// Drain constant setup commands after all user command factories have run.
/// This is consumed by the export pipeline, not by user code.
#[doc(hidden)]
pub fn drain_constant_setup() -> Vec<String> {
    let mut registry = constants_registry()
        .lock()
        .expect("score constant registry poisoned");
    let entries = std::mem::take(&mut *registry);
    let mut objectives = entries
        .keys()
        .map(|(objective, _)| objective.clone())
        .collect::<Vec<_>>();
    objectives.sort();
    objectives.dedup();
    let mut commands: Vec<String> = objectives
        .into_iter()
        .map(|objective| format!("scoreboard objectives add {objective} dummy"))
        .collect();
    commands.extend(entries.into_iter().map(|((objective, holder), value)| {
        format!("scoreboard players set {holder} {objective} {value}")
    }));
    commands
}

pub(crate) fn request_expression_temp() {
    crate::function::request_internal_score_temp();
}

/// Drain all internally managed score setup. Used by the exporter.
#[doc(hidden)]
pub fn drain_internal_score_setup() -> Vec<String> {
    let mut commands = drain_constant_setup();
    if crate::function::take_internal_score_temp_request() {
        commands.insert(
            0,
            format!("scoreboard objectives add {SCORE_EXPRESSION_TEMP_OBJECTIVE} dummy"),
        );
    }
    commands
}

/// Sand's compiler-managed temporary objective used by score expressions.
pub(crate) const SCORE_EXPRESSION_TEMP_OBJECTIVE: &str = "__sand_tmp";

// ── ScoreVar ──────────────────────────────────────────────────────────────────

/// A typed scoreboard variable backed by a single scoreboard objective.
///
/// Declare once as a `static` and use throughout your datapack:
///
/// ```rust,ignore
/// use sand_core::state::ScoreVar;
///
/// static MANA: ScoreVar<i32> = ScoreVar::new("mana");
///
/// let cmds = vec![
///     MANA.define(),
///     MANA.set("@s", 100),
///     MANA.add("@s", 5),
/// ];
/// ```
#[sand_macros::api(
    registry = sand_api_contract,
    path = "sand::state::ScoreVar",
    aliases = ["sand::prelude::ScoreVar"],
    module = "sand::state",
    summary = "A typed scoreboard variable backed by a single scoreboard objective.",
    context = "A typed scoreboard variable backed by a single scoreboard objective. Declare once as a `static` and use throughout your datapack:",
    minecraft = "Declare once as a `static` and use throughout your datapack:",
    use_when = ["Working with typed gameplay state or composing state transitions"],
    avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
    example = "use sand::state::ScoreVar;",
)]
pub struct ScoreVar<T = i32> {
    name: &'static str,
    _marker: PhantomData<T>,
}

impl<T> ScoreVar<T> {
    /// Create a new `ScoreVar` with the given objective name.
    ///
    /// Names longer than 16 characters are automatically hashed to a stable
    /// 16-character objective name (see [`ScoreVar::objective_name`]).
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreVar::new",
        aliases = ["sand::prelude::ScoreVar::new"],
        module = "sand::state",
        kind = "method",
        summary = "Create a new `ScoreVar` with the given objective name.",
        context = "Create a new `ScoreVar` with the given objective name. Names longer than 16 characters are automatically hashed to a stable 16-character objective name (see [`ScoreVar::objective_name`]).",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(name = "`name` provides the author-visible text value used to create a new `ScoreVar` with the given objective name."),
        returns = "A newly constructed `ScoreVar` configured to create a new `ScoreVar` with the given objective name.",
        example = "use sand::prelude::*;\n\nfn demonstrate<T: 'static>(name: & 'static str)  {\n    let score_var = sand::state::ScoreVar ::< T >::new(name);\n}",
    )]
    pub const fn new(name: &'static str) -> Self {
        Self {
            name,
            _marker: PhantomData,
        }
    }

    /// Return the actual scoreboard objective name used in commands.
    ///
    /// This is either `name` directly (≤16 chars) or a stable hash (>16 chars).
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreVar::objective_name",
        aliases = ["sand::prelude::ScoreVar::objective_name"],
        module = "sand::state",
        kind = "method",
        summary = "Return the actual scoreboard objective name used in commands.",
        context = "Return the actual scoreboard objective name used in commands. This is either `name` directly (≤16 chars) or a stable hash (>16 chars).",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        returns = "Return the actual scoreboard objective name used in commands.",
        example = "use sand::prelude::*;\n\nfn demonstrate<T: 'static>(score_var_value: &sand::state::ScoreVar < T >)  {\n    let command = score_var_value.objective_name();\n}",
    )]
    pub fn objective_name(&self) -> String {
        objective_name(self.name)
    }

    /// `scoreboard objectives add <obj> dummy` — register the objective.
    ///
    /// Call this in your `load` function.
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreVar::define",
        aliases = ["sand::prelude::ScoreVar::define"],
        module = "sand::state",
        kind = "method",
        summary = "`scoreboard objectives add <obj> dummy` — register the objective.",
        context = "`scoreboard objectives add <obj> dummy` — register the objective. Call this in your `load` function.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Call this in your `load` function."],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        returns = "The string value produced to emit the documented `scoreboard objectives add <obj> dummy` — register the objective form.",
        example = "use sand::prelude::*;\n\nfn demonstrate<T: 'static>(score_var_value: &sand::state::ScoreVar < T >)  {\n    let define = score_var_value.define();\n}",
    )]
    pub fn define(&self) -> String {
        format!("scoreboard objectives add {} dummy", self.objective_name())
    }

    /// `scoreboard players set <selector> <obj> <value>`
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreVar::set",
        aliases = ["sand::prelude::ScoreVar::set"],
        module = "sand::state",
        kind = "method",
        summary = "`scoreboard players set <selector> <obj> <value>`",
        context = "`scoreboard players set <selector> <obj> <value>` This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(selector = "`selector` provides the Minecraft target selection used to emit the documented `scoreboard players set <selector> <obj> <value>` form.", value = "`value` provides the value being applied or compared used to emit the documented `scoreboard players set <selector> <obj> <value>` form."),
        returns = "The string value produced to emit the documented `scoreboard players set <selector> <obj> <value>` form.",
        example = "use std::fmt;\nuse sand::prelude::*;\n\nfn demonstrate<T: 'static>(score_var_value: &sand::state::ScoreVar < T >, selector: impl std::fmt::Display, value: i32)  {\n    let set = score_var_value.set(selector, value);\n}",
    )]
    pub fn set(&self, selector: impl std::fmt::Display, value: i32) -> String {
        format!(
            "scoreboard players set {} {} {}",
            selector,
            self.objective_name(),
            value
        )
    }

    /// `scoreboard players add <selector> <obj> <amount>`
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreVar::add",
        aliases = ["sand::prelude::ScoreVar::add"],
        module = "sand::state",
        kind = "method",
        summary = "`scoreboard players add <selector> <obj> <amount>`",
        context = "`scoreboard players add <selector> <obj> <amount>` This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(selector = "`selector` provides the Minecraft target selection used to emit the documented `scoreboard players add <selector> <obj> <amount>` form.", amount = "`amount` provides the requested numeric amount used to emit the documented `scoreboard players add <selector> <obj> <amount>` form."),
        returns = "The string value produced to emit the documented `scoreboard players add <selector> <obj> <amount>` form.",
        example = "use std::fmt;\nuse sand::prelude::*;\n\nfn demonstrate<T: 'static>(score_var_value: &sand::state::ScoreVar < T >, selector: impl std::fmt::Display, amount: i32)  {\n    let add = score_var_value.add(selector, amount);\n}",
    )]
    pub fn add(&self, selector: impl std::fmt::Display, amount: i32) -> String {
        format!(
            "scoreboard players add {} {} {}",
            selector,
            self.objective_name(),
            amount
        )
    }

    /// `scoreboard players remove <selector> <obj> <amount>`
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreVar::remove",
        aliases = ["sand::prelude::ScoreVar::remove"],
        module = "sand::state",
        kind = "method",
        summary = "`scoreboard players remove <selector> <obj> <amount>`",
        context = "`scoreboard players remove <selector> <obj> <amount>` This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(selector = "`selector` provides the Minecraft target selection used to emit the documented `scoreboard players remove <selector> <obj> <amount>` form.", amount = "`amount` provides the requested numeric amount used to emit the documented `scoreboard players remove <selector> <obj> <amount>` form."),
        returns = "The string value produced to emit the documented `scoreboard players remove <selector> <obj> <amount>` form.",
        example = "use std::fmt;\nuse sand::prelude::*;\n\nfn demonstrate<T: 'static>(score_var_value: &sand::state::ScoreVar < T >, selector: impl std::fmt::Display, amount: i32)  {\n    let remove = score_var_value.remove(selector, amount);\n}",
    )]
    pub fn remove(&self, selector: impl std::fmt::Display, amount: i32) -> String {
        format!(
            "scoreboard players remove {} {} {}",
            selector,
            self.objective_name(),
            amount
        )
    }

    /// `scoreboard players reset <selector> <obj>`
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreVar::reset",
        aliases = ["sand::prelude::ScoreVar::reset"],
        module = "sand::state",
        kind = "method",
        summary = "`scoreboard players reset <selector> <obj>`",
        context = "`scoreboard players reset <selector> <obj>` This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(selector = "`selector` provides the Minecraft target selection used to emit the documented `scoreboard players reset <selector> <obj>` form."),
        returns = "The string value produced to emit the documented `scoreboard players reset <selector> <obj>` form.",
        example = "use std::fmt;\nuse sand::prelude::*;\n\nfn demonstrate<T: 'static>(score_var_value: &sand::state::ScoreVar < T >, selector: impl std::fmt::Display)  {\n    let reset = score_var_value.reset(selector);\n}",
    )]
    pub fn reset(&self, selector: impl std::fmt::Display) -> String {
        format!(
            "scoreboard players reset {} {}",
            selector,
            self.objective_name()
        )
    }

    /// Clamp the score for `selector` to `[min, max]`.
    ///
    /// Returns two commands: one to enforce the lower bound and one for the upper.
    ///
    /// Generated commands:
    /// ```text
    /// execute if score <selector> <obj> matches ..<min-1> run scoreboard players set <selector> <obj> <min>
    /// execute if score <selector> <obj> matches <max+1>.. run scoreboard players set <selector> <obj> <max>
    /// ```
    ///
    /// # `min > max`
    ///
    /// This infallible method does not reject `min > max` — it stays
    /// available so existing valid call sites keep byte-identical output.
    /// Two contradictory commands are emitted in that case (see
    /// [#146](https://github.com/ThatOneToast/sand/issues/146)). Prefer
    /// [`ScoreVar::try_clamp`], which rejects `min > max` before returning
    /// any command text.
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreVar::clamp",
        aliases = ["sand::prelude::ScoreVar::clamp"],
        module = "sand::state",
        kind = "method",
        summary = "Clamp the score for `selector` to `[min, max]`. Returns two commands: one to enforce the lower bound and one for the upper.",
        context = "Clamp the score for `selector` to `[min, max]`. Returns two commands: one to enforce the lower bound and one for the upper. Generated commands: This infallible method does not reject `min > max` — it stays available so existing valid call sites keep byte-identical output. Two contradictory commands are emitted in that case (see [#146](https://github.com/ThatOneToast/sand/issues/146)). Prefer [`ScoreVar::try_clamp`], which rejects `min > max` before returning any command text.",
        minecraft = "Returns two commands: one to enforce the lower bound and one for the upper.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(selector = "Clamp the score for `selector` to `[min, max]`.", min = "`min` provides the inclusive lower bound used to clamp the score for `selector` to `[min, max]`. Returns two commands: one to enforce the lower bound and one for the upper.", max = "`max` provides the inclusive upper bound used to clamp the score for `selector` to `[min, max]`. Returns two commands: one to enforce the lower bound and one for the upper."),
        returns = "Returns two commands: one to enforce the lower bound and one for the upper.",
        example = "use std::fmt;\nuse sand::prelude::*;\n\nfn demonstrate<T: 'static>(score_var_value: &sand::state::ScoreVar < T >, selector: impl std::fmt::Display, min: i32, max: i32)  {\n    let values = score_var_value.clamp(selector, min, max);\n}",
    )]
    pub fn clamp(&self, selector: impl std::fmt::Display, min: i32, max: i32) -> Vec<String> {
        let selector = selector.to_string();
        let obj = self.objective_name();
        vec![
            format!(
                "execute if score {selector} {obj} matches ..{} run scoreboard players set {selector} {obj} {min}",
                min.saturating_sub(1)
            ),
            format!(
                "execute if score {selector} {obj} matches {}.. run scoreboard players set {selector} {obj} {max}",
                max.saturating_add(1)
            ),
        ]
    }

    /// Validated counterpart to [`ScoreVar::clamp`] — rejects `min > max`
    /// before generating any mcfunction output instead of emitting two
    /// contradictory `execute if score ... matches` commands.
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreVar::try_clamp",
        aliases = ["sand::prelude::ScoreVar::try_clamp"],
        module = "sand::state",
        kind = "method",
        summary = "Validated counterpart to [`ScoreVar::clamp`] — rejects `min > max` before generating any mcfunction output instead of emitting two contradictory `execute if score ... matches` commands.",
        context = "Validated counterpart to [`ScoreVar::clamp`] — rejects `min > max` before generating any mcfunction output instead of emitting two contradictory `execute if score ... matches` commands. This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(selector = "`selector` provides the Minecraft target selection used to use validated counterpart to [`ScoreVar::clamp`] — rejects `min > max` before generating any mcfunction output instead of emitting two contradictory `execute if score ... matches` commands.", min = "`min` provides the inclusive lower bound used to use validated counterpart to [`ScoreVar::clamp`] — rejects `min > max` before generating any mcfunction output instead of emitting two contradictory `execute if score ... matches` commands.", max = "`max` provides the inclusive upper bound used to use validated counterpart to [`ScoreVar::clamp`] — rejects `min > max` before generating any mcfunction output instead of emitting two contradictory `execute if score ... matches` commands."),
        returns = "The `sand :: command :: CommandResult < Vec < String > >` value produced to use validated counterpart to [`ScoreVar::clamp`] — rejects `min > max` before generating any mcfunction output instead of emitting two contradictory `execute if score ... matches` commands.",
        example = "use std::fmt;\nuse sand::prelude::*;\n\nfn demonstrate<T: 'static>(score_var_value: &sand::state::ScoreVar < T >, selector: impl std::fmt::Display, min: i32, max: i32)  {\n    let try_clamp = score_var_value.try_clamp(selector, min, max);\n}",
    )]
    pub fn try_clamp(
        &self,
        selector: impl std::fmt::Display,
        min: i32,
        max: i32,
    ) -> sand_commands::CommandResult<Vec<String>> {
        if min > max {
            return Err(sand_commands::CommandError::new(
                "ScoreVar::try_clamp",
                "min_max",
                format!("min ({min}) must not be greater than max ({max})"),
            )
            .with_code("SAND-SCORE-RANGE"));
        }
        Ok(self.clamp(selector, min, max))
    }

    /// Bind this variable to a selector to produce a condition builder.
    ///
    /// Compatibility/raw path: `selector` is an unvalidated string,
    /// interpolated directly into generated commands. Prefer
    /// [`ScoreVar::try_of`] in normal code — see
    /// [#146](https://github.com/ThatOneToast/sand/issues/146).
    ///
    /// ```rust,ignore
    /// let cond = MANA.of("@s").gte(25);
    /// ```
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreVar::of",
        aliases = ["sand::prelude::ScoreVar::of"],
        module = "sand::state",
        kind = "method",
        summary = "Bind this variable to a selector to produce a condition builder.",
        context = "Bind this variable to a selector to produce a condition builder. Compatibility/raw path: `selector` is an unvalidated string, interpolated directly into generated commands. Prefer [`ScoreVar::try_of`] in normal code — see [#146](https://github.com/ThatOneToast/sand/issues/146).",
        minecraft = "Compatibility/raw path: `selector` is an unvalidated string, interpolated directly into generated commands. Prefer [`ScoreVar::try_of`] in normal code — see [#146](https://github.com/ThatOneToast/sand/issues/146).",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(selector = "Compatibility/raw path: `selector` is an unvalidated string, interpolated directly into generated commands. Prefer [`ScoreVar::try_of`] in normal code — see [#146](https://github.com/ThatOneToast/sand/issues/146)."),
        returns = "The `ScoreRef < 'a , T >` value produced to bind this variable to a selector to produce a condition builder.",
        example = "let cond = MANA.of(\"@s\").gte(25);",
    )]
    pub fn of<'a>(&'a self, selector: &str) -> ScoreRef<'a, T> {
        ScoreRef {
            objective: self.name,
            selector: selector.to_string(),
            _marker: PhantomData,
        }
    }

    /// Validated counterpart to [`ScoreVar::of`] — takes a typed
    /// [`sand_commands::ScoreHolder`] (an entity selector or a fake player)
    /// and validates it before producing a condition builder, instead of
    /// interpolating an unvalidated selector string.
    ///
    /// ```
    /// use sand_core::state::ScoreVar;
    /// use sand_commands::ScoreHolder;
    ///
    /// static MANA: ScoreVar<i32> = ScoreVar::new("mana");
    ///
    /// let cond = MANA.try_of(ScoreHolder::self_()).unwrap().gte(25);
    /// assert!(MANA.try_of(ScoreHolder::fake("bad holder")).is_err());
    /// ```
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreVar::try_of",
        aliases = ["sand::prelude::ScoreVar::try_of"],
        module = "sand::state",
        kind = "method",
        summary = "Validated counterpart to [`ScoreVar::of`] — takes a typed [`sand::command::ScoreHolder`] (an entity selector or a fake player) and validates it before producing a condition builder, instead of interpolating an unvalidated selector string.",
        context = "Validated counterpart to [`ScoreVar::of`] — takes a typed [`sand::command::ScoreHolder`] (an entity selector or a fake player) and validates it before producing a condition builder, instead of interpolating an unvalidated selector string. This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(holder = "`holder` supplies the holder value used to use validated counterpart to [`ScoreVar::of`] — takes a typed [`sand::command::ScoreHolder`] (an entity selector or a fake player) and validates it before producing a condition builder, instead of interpolating an unvalidated selector string."),
        returns = "The `sand :: command :: CommandResult < ScoreRef < 'a , T > >` value produced to use validated counterpart to [`ScoreVar::of`] — takes a typed [`sand::command::ScoreHolder`] (an entity selector or a fake player) and validates it before producing a condition builder, instead of interpolating an unvalidated selector string.",
        example = "use sand::state::ScoreVar;\nuse sand::command::ScoreHolder;\nstatic MANA: ScoreVar<i32> = ScoreVar::new(\"mana\");\nlet cond = MANA.try_of(ScoreHolder::self_()).unwrap().gte(25);\nassert!(MANA.try_of(ScoreHolder::fake(\"bad holder\")).is_err());",
    )]
    pub fn try_of<'a>(
        &'a self,
        holder: impl Into<sand_commands::ScoreHolder>,
    ) -> sand_commands::CommandResult<ScoreRef<'a, T>> {
        let holder = holder.into();
        // `execute if/unless score <holder> ...` requires exactly one score
        // holder — a wildcard or a multi-entity selector is not legal here,
        // even though it is legal for `set`/`add`/`remove`/`reset`/`init`.
        holder.validate_single(&sand_commands::CommandProfile::unprofiled())?;
        Ok(ScoreRef {
            objective: self.name,
            selector: holder.to_string(),
            _marker: PhantomData,
        })
    }

    /// Validated counterpart to [`ScoreVar::set`] — takes a typed
    /// [`sand_commands::ScoreHolder`] and validates it before generating the
    /// `scoreboard players set` command, instead of interpolating an
    /// unvalidated `Display` value. See
    /// [#146](https://github.com/ThatOneToast/sand/issues/146).
    ///
    /// ```
    /// use sand_core::state::ScoreVar;
    /// use sand_commands::ScoreHolder;
    ///
    /// static MANA: ScoreVar<i32> = ScoreVar::new("mana");
    ///
    /// assert_eq!(
    ///     MANA.try_set(ScoreHolder::self_(), 100).unwrap(),
    ///     "scoreboard players set @s mana 100"
    /// );
    /// assert!(MANA.try_set(ScoreHolder::fake("bad holder"), 100).is_err());
    /// ```
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreVar::try_set",
        aliases = ["sand::prelude::ScoreVar::try_set"],
        module = "sand::state",
        kind = "method",
        summary = "Validated counterpart to [`ScoreVar::set`] — takes a typed [`sand::command::ScoreHolder`] and validates it before generating the `scoreboard players set` command, instead of interpolating an unvalidated `Display` value. See [#146](https://github.com/ThatOneToast/sand/issues/146).",
        context = "Validated counterpart to [`ScoreVar::set`] — takes a typed [`sand::command::ScoreHolder`] and validates it before generating the `scoreboard players set` command, instead of interpolating an unvalidated `Display` value. See [#146](https://github.com/ThatOneToast/sand/issues/146). This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(holder = "`holder` supplies the holder value used to use validated counterpart to [`ScoreVar::set`] — takes a typed [`sand::command::ScoreHolder`] and validates it before generating the `scoreboard players set` command, instead of interpolating an unvalidated `Display` value. See [#146](https://github.com/ThatOneToast/sand/issues/146).", value = "`value` provides the value being applied or compared used to use validated counterpart to [`ScoreVar::set`] — takes a typed [`sand::command::ScoreHolder`] and validates it before generating the `scoreboard players set` command, instead of interpolating an unvalidated `Display` value. See [#146](https://github.com/ThatOneToast/sand/issues/146)."),
        returns = "The `sand :: command :: CommandResult < String >` value produced to use validated counterpart to [`ScoreVar::set`] — takes a typed [`sand::command::ScoreHolder`] and validates it before generating the `scoreboard players set` command, instead of interpolating an unvalidated `Display` value. See [#146](https://github.com/ThatOneToast/sand/issues/146).",
        example = "use sand::state::ScoreVar;\nuse sand::command::ScoreHolder;\nstatic MANA: ScoreVar<i32> = ScoreVar::new(\"mana\");\nassert_eq!(\nMANA.try_set(ScoreHolder::self_(), 100).unwrap(),\n\"scoreboard players set @s mana 100\"\n);\nassert!(MANA.try_set(ScoreHolder::fake(\"bad holder\"), 100).is_err());",
    )]
    pub fn try_set(
        &self,
        holder: impl Into<sand_commands::ScoreHolder>,
        value: i32,
    ) -> sand_commands::CommandResult<String> {
        let holder = holder.into();
        sand_commands::Validate::validate(&holder, &sand_commands::CommandProfile::unprofiled())?;
        Ok(self.set(holder.to_string(), value))
    }

    /// Validated counterpart to [`ScoreVar::add`] — see [`ScoreVar::try_set`].
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreVar::try_add",
        aliases = ["sand::prelude::ScoreVar::try_add"],
        module = "sand::state",
        kind = "method",
        summary = "Validated counterpart to [`ScoreVar::add`] — see [`ScoreVar::try_set`].",
        context = "Validated counterpart to [`ScoreVar::add`] — see [`ScoreVar::try_set`]. This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(holder = "`holder` supplies the holder value used to use validated counterpart to [`ScoreVar::add`] — see [`ScoreVar::try_set`].", amount = "`amount` provides the requested numeric amount used to use validated counterpart to [`ScoreVar::add`] — see [`ScoreVar::try_set`]."),
        returns = "The `sand :: command :: CommandResult < String >` value produced to use validated counterpart to [`ScoreVar::add`] — see [`ScoreVar::try_set`].",
        example = "use sand::prelude::*;\n\nfn demonstrate<T: 'static>(score_var_value: &sand::state::ScoreVar < T >, holder: impl Into < sand::command::ScoreHolder >, amount: i32)  {\n    let try_add = score_var_value.try_add(holder, amount);\n}",
    )]
    pub fn try_add(
        &self,
        holder: impl Into<sand_commands::ScoreHolder>,
        amount: i32,
    ) -> sand_commands::CommandResult<String> {
        let holder = holder.into();
        sand_commands::Validate::validate(&holder, &sand_commands::CommandProfile::unprofiled())?;
        Ok(self.add(holder.to_string(), amount))
    }

    /// Validated counterpart to [`ScoreVar::remove`] — see [`ScoreVar::try_set`].
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreVar::try_remove",
        aliases = ["sand::prelude::ScoreVar::try_remove"],
        module = "sand::state",
        kind = "method",
        summary = "Validated counterpart to [`ScoreVar::remove`] — see [`ScoreVar::try_set`].",
        context = "Validated counterpart to [`ScoreVar::remove`] — see [`ScoreVar::try_set`]. This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(holder = "`holder` supplies the holder value used to use validated counterpart to [`ScoreVar::remove`] — see [`ScoreVar::try_set`].", amount = "`amount` provides the requested numeric amount used to use validated counterpart to [`ScoreVar::remove`] — see [`ScoreVar::try_set`]."),
        returns = "The `sand :: command :: CommandResult < String >` value produced to use validated counterpart to [`ScoreVar::remove`] — see [`ScoreVar::try_set`].",
        example = "use sand::prelude::*;\n\nfn demonstrate<T: 'static>(score_var_value: &sand::state::ScoreVar < T >, holder: impl Into < sand::command::ScoreHolder >, amount: i32)  {\n    let try_remove = score_var_value.try_remove(holder, amount);\n}",
    )]
    pub fn try_remove(
        &self,
        holder: impl Into<sand_commands::ScoreHolder>,
        amount: i32,
    ) -> sand_commands::CommandResult<String> {
        let holder = holder.into();
        sand_commands::Validate::validate(&holder, &sand_commands::CommandProfile::unprofiled())?;
        Ok(self.remove(holder.to_string(), amount))
    }

    /// Validated counterpart to [`ScoreVar::reset`] — see [`ScoreVar::try_set`].
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreVar::try_reset",
        aliases = ["sand::prelude::ScoreVar::try_reset"],
        module = "sand::state",
        kind = "method",
        summary = "Validated counterpart to [`ScoreVar::reset`] — see [`ScoreVar::try_set`].",
        context = "Validated counterpart to [`ScoreVar::reset`] — see [`ScoreVar::try_set`]. This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(holder = "`holder` supplies the holder value used to use validated counterpart to [`ScoreVar::reset`] — see [`ScoreVar::try_set`]."),
        returns = "The `sand :: command :: CommandResult < String >` value produced to use validated counterpart to [`ScoreVar::reset`] — see [`ScoreVar::try_set`].",
        example = "use sand::prelude::*;\n\nfn demonstrate<T: 'static>(score_var_value: &sand::state::ScoreVar < T >, holder: impl Into < sand::command::ScoreHolder >)  {\n    let try_reset = score_var_value.try_reset(holder);\n}",
    )]
    pub fn try_reset(
        &self,
        holder: impl Into<sand_commands::ScoreHolder>,
    ) -> sand_commands::CommandResult<String> {
        let holder = holder.into();
        sand_commands::Validate::validate(&holder, &sand_commands::CommandProfile::unprofiled())?;
        Ok(self.reset(holder.to_string()))
    }

    /// Validated counterpart to [`ScoreVar::init`] — see [`ScoreVar::try_set`].
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreVar::try_init",
        aliases = ["sand::prelude::ScoreVar::try_init"],
        module = "sand::state",
        kind = "method",
        summary = "Validated counterpart to [`ScoreVar::init`] — see [`ScoreVar::try_set`].",
        context = "Validated counterpart to [`ScoreVar::init`] — see [`ScoreVar::try_set`]. This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(holder = "`holder` supplies the holder value used to use validated counterpart to [`ScoreVar::init`] — see [`ScoreVar::try_set`].", value = "`value` provides the value being applied or compared used to use validated counterpart to [`ScoreVar::init`] — see [`ScoreVar::try_set`]."),
        returns = "The `sand :: command :: CommandResult < String >` value produced to use validated counterpart to [`ScoreVar::init`] — see [`ScoreVar::try_set`].",
        example = "use sand::prelude::*;\n\nfn demonstrate<T: 'static>(score_var_value: &sand::state::ScoreVar < T >, holder: impl Into < sand::command::ScoreHolder >, value: i32)  {\n    let try_init = score_var_value.try_init(holder, value);\n}",
    )]
    pub fn try_init(
        &self,
        holder: impl Into<sand_commands::ScoreHolder>,
        value: i32,
    ) -> sand_commands::CommandResult<String> {
        let holder = holder.into();
        sand_commands::Validate::validate(&holder, &sand_commands::CommandProfile::unprofiled())?;
        Ok(self.init(holder.to_string(), value))
    }

    /// Validated counterpart to [`ScoreVar::copy_within`] — see
    /// [`ScoreVar::try_set`]. Both the source and destination holders are
    /// validated before any command text is generated.
    ///
    /// `scoreboard players operation <dst> <obj> = <src> <obj>` allows
    /// `dst` to resolve to multiple holders (the operation runs once per
    /// holder) but requires `src` — the copy source — to resolve to exactly
    /// one holder, matching vanilla's `scoreboard players operation`
    /// semantics. `src_holder` is validated with
    /// [`sand_commands::ScoreHolder::validate_single`] accordingly; a
    /// wildcard or multi-entity selector source is rejected.
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreVar::try_copy_within",
        aliases = ["sand::prelude::ScoreVar::try_copy_within"],
        module = "sand::state",
        kind = "method",
        summary = "Validated counterpart to [`ScoreVar::copy_within`] — see [`ScoreVar::try_set`]. Both the source and destination holders are validated before any command text is generated.",
        context = "Validated counterpart to [`ScoreVar::copy_within`] — see [`ScoreVar::try_set`]. Both the source and destination holders are validated before any command text is generated. `scoreboard players operation <dst> <obj> = <src> <obj>` allows `dst` to resolve to multiple holders (the operation runs once per holder) but requires `src` — the copy source — to resolve to exactly one holder, matching vanilla's `scoreboard players operation` semantics. `src_holder` is validated with [`sand::command::ScoreHolder::validate_single`] accordingly; a wildcard or multi-entity selector source is rejected.",
        minecraft = "`scoreboard players operation <dst> <obj> = <src> <obj>` allows `dst` to resolve to multiple holders (the operation runs once per holder) but requires `src` — the copy source — to resolve to exactly one holder, matching vanilla's `scoreboard players operation` semantics. `src_holder` is validated with [`sand::command::ScoreHolder::validate_single`] accordingly; a wildcard or multi-entity selector source is rejected.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(src_holder = "`scoreboard players operation <dst> <obj> = <src> <obj>` allows `dst` to resolve to multiple holders (the operation runs once per holder) but requires `src` — the copy source — to resolve to exactly one holder, matching vanilla's `scoreboard players operation` semantics. `src_holder` is validated with [`sand::command::ScoreHolder::validate_single`] accordingly; a wildcard or multi-entity selector source is rejected.", dst_holder = "`dst_holder` supplies the dst holder value used to use validated counterpart to [`ScoreVar::copy_within`] — see [`ScoreVar::try_set`]. Both the source and destination holders are validated before any command text is generated."),
        returns = "The `sand :: command :: CommandResult < String >` value produced to use validated counterpart to [`ScoreVar::copy_within`] — see [`ScoreVar::try_set`]. Both the source and destination holders are validated before any command text is generated.",
        example = "use sand::prelude::*;\n\nfn demonstrate<T: 'static>(score_var_value: &sand::state::ScoreVar < T >, src_holder: impl Into < sand::command::ScoreHolder >, dst_holder: impl Into < sand::command::ScoreHolder >)  {\n    let try_copy_within = score_var_value.try_copy_within(src_holder, dst_holder);\n}",
    )]
    pub fn try_copy_within(
        &self,
        src_holder: impl Into<sand_commands::ScoreHolder>,
        dst_holder: impl Into<sand_commands::ScoreHolder>,
    ) -> sand_commands::CommandResult<String> {
        let src_holder = src_holder.into();
        let dst_holder = dst_holder.into();
        src_holder.validate_single(&sand_commands::CommandProfile::unprofiled())?;
        sand_commands::Validate::validate(
            &dst_holder,
            &sand_commands::CommandProfile::unprofiled(),
        )?;
        Ok(self.copy_within(src_holder.to_string(), dst_holder.to_string()))
    }

    /// Initialize the score to `value` only if the selector has no existing score entry.
    ///
    /// Uses `unless score … matches -2147483648..` to detect a missing score.
    ///
    /// Generated command:
    /// ```text
    /// execute unless score <selector> <obj> matches -2147483648.. run scoreboard players set <selector> <obj> <value>
    /// ```
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreVar::init",
        aliases = ["sand::prelude::ScoreVar::init"],
        module = "sand::state",
        kind = "method",
        summary = "Initialize the score to `value` only if the selector has no existing score entry.",
        context = "Initialize the score to `value` only if the selector has no existing score entry. Uses `unless score … matches -2147483648..` to detect a missing score. Generated command:",
        minecraft = "Generated command:",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(selector = "`selector` provides the Minecraft target selection used to initialize the score to `value` only if the selector has no existing score entry.", value = "Initialize the score to `value` only if the selector has no existing score entry."),
        returns = "The string value produced to initialize the score to `value` only if the selector has no existing score entry.",
        example = "use std::fmt;\nuse sand::prelude::*;\n\nfn demonstrate<T: 'static>(score_var_value: &sand::state::ScoreVar < T >, selector: impl std::fmt::Display, value: i32)  {\n    let init = score_var_value.init(selector, value);\n}",
    )]
    pub fn init(&self, selector: impl std::fmt::Display, value: i32) -> String {
        format!(
            "execute unless score {} {} matches -2147483648.. run scoreboard players set {} {} {}",
            selector,
            self.objective_name(),
            selector,
            self.objective_name(),
            value
        )
    }

    /// Copy this score from `src_selector` to `dst_selector` within the same objective.
    ///
    /// Generated command:
    /// ```text
    /// scoreboard players operation <dst> <obj> = <src> <obj>
    /// ```
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreVar::copy_within",
        aliases = ["sand::prelude::ScoreVar::copy_within"],
        module = "sand::state",
        kind = "method",
        summary = "Copy this score from `src_selector` to `dst_selector` within the same objective.",
        context = "Copy this score from `src_selector` to `dst_selector` within the same objective. Generated command:",
        minecraft = "Generated command:",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(src_selector = "Copy this score from `src_selector` to `dst_selector` within the same objective.", dst_selector = "Copy this score from `src_selector` to `dst_selector` within the same objective."),
        returns = "The string value produced to copy this score from `src_selector` to `dst_selector` within the same objective.",
        example = "use std::fmt;\nuse sand::prelude::*;\n\nfn demonstrate<T: 'static>(score_var_value: &sand::state::ScoreVar < T >, src_selector: impl std::fmt::Display, dst_selector: impl std::fmt::Display)  {\n    let copy_within = score_var_value.copy_within(src_selector, dst_selector);\n}",
    )]
    pub fn copy_within(
        &self,
        src_selector: impl std::fmt::Display,
        dst_selector: impl std::fmt::Display,
    ) -> String {
        format!(
            "scoreboard players operation {} {} = {} {}",
            dst_selector,
            self.objective_name(),
            src_selector,
            self.objective_name()
        )
    }

    /// Copy a score from another `ScoreVar` into this one for `selector`.
    ///
    /// Generated command:
    /// ```text
    /// scoreboard players operation <dst_sel> <self_obj> = <src_sel> <src_obj>
    /// ```
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreVar::copy_from",
        aliases = ["sand::prelude::ScoreVar::copy_from"],
        module = "sand::state",
        kind = "method",
        summary = "Copy a score from another `ScoreVar` into this one for `selector`.",
        context = "Copy a score from another `ScoreVar` into this one for `selector`. Generated command:",
        minecraft = "Generated command:",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(dst_selector = "`dst_selector` supplies the dst selector value used to copy a score from another `ScoreVar` into this one for `selector`.", src = "`src` supplies the src value used to copy a score from another `ScoreVar` into this one for `selector`.", src_selector = "`src_selector` supplies the src selector value used to copy a score from another `ScoreVar` into this one for `selector`."),
        returns = "The string value produced to copy a score from another `ScoreVar` into this one for `selector`.",
        example = "use std::fmt;\nuse sand::prelude::*;\n\nfn demonstrate<T: 'static, U: 'static>(score_var_value: &sand::state::ScoreVar < T >, dst_selector: impl std::fmt::Display, src: & sand::state::ScoreVar < U >, src_selector: impl std::fmt::Display)  {\n    let copy_from = score_var_value.copy_from::<U>(dst_selector, src, src_selector);\n}",
    )]
    pub fn copy_from<U>(
        &self,
        dst_selector: impl std::fmt::Display,
        src: &ScoreVar<U>,
        src_selector: impl std::fmt::Display,
    ) -> String {
        format!(
            "scoreboard players operation {} {} = {} {}",
            dst_selector,
            self.objective_name(),
            src_selector,
            src.objective_name()
        )
    }

    /// Copy this score into another `ScoreVar`.
    ///
    /// Generated command:
    /// ```text
    /// scoreboard players operation <dst_sel> <dst_obj> = <src_sel> <self_obj>
    /// ```
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreVar::copy_to",
        aliases = ["sand::prelude::ScoreVar::copy_to"],
        module = "sand::state",
        kind = "method",
        summary = "Copy this score into another `ScoreVar`. Generated command:.",
        context = "Copy this score into another `ScoreVar`. Generated command:",
        minecraft = "Generated command:",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(src_selector = "`src_selector` supplies the src selector value used to copy this score into another `ScoreVar`. Generated command:.", dst = "`dst` supplies the dst value used to copy this score into another `ScoreVar`. Generated command:.", dst_selector = "`dst_selector` supplies the dst selector value used to copy this score into another `ScoreVar`. Generated command:."),
        returns = "The rendered Minecraft command text produced to copy this score into another `ScoreVar`. Generated command:.",
        example = "use std::fmt;\nuse sand::prelude::*;\n\nfn demonstrate<T: 'static, U: 'static>(score_var_value: &sand::state::ScoreVar < T >, src_selector: impl std::fmt::Display, dst: & sand::state::ScoreVar < U >, dst_selector: impl std::fmt::Display)  {\n    let command = score_var_value.copy_to::<U>(src_selector, dst, dst_selector);\n}",
    )]
    pub fn copy_to<U>(
        &self,
        src_selector: impl std::fmt::Display,
        dst: &ScoreVar<U>,
        dst_selector: impl std::fmt::Display,
    ) -> String {
        format!(
            "scoreboard players operation {} {} = {} {}",
            dst_selector,
            dst.objective_name(),
            src_selector,
            self.objective_name()
        )
    }

    /// Set this score to the minimum of itself and another variable.
    ///
    /// Generated command:
    /// ```text
    /// scoreboard players operation <sel> <self_obj> < <other_sel> <other_obj>
    /// ```
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreVar::min_op",
        aliases = ["sand::prelude::ScoreVar::min_op"],
        module = "sand::state",
        kind = "method",
        summary = "Set this score to the minimum of itself and another variable.",
        context = "Set this score to the minimum of itself and another variable. Generated command:",
        minecraft = "Generated command:",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(selector = "`selector` provides the Minecraft target selection used to set this score to the minimum of itself and another variable.", other = "`other` supplies the other value used to set this score to the minimum of itself and another variable.", other_selector = "`other_selector` supplies the other selector value used to set this score to the minimum of itself and another variable."),
        returns = "The string value produced to set this score to the minimum of itself and another variable.",
        example = "use std::fmt;\nuse sand::prelude::*;\n\nfn demonstrate<T: 'static, U: 'static>(score_var_value: &sand::state::ScoreVar < T >, selector: impl std::fmt::Display, other: & sand::state::ScoreVar < U >, other_selector: impl std::fmt::Display)  {\n    let min_op = score_var_value.min_op::<U>(selector, other, other_selector);\n}",
    )]
    pub fn min_op<U>(
        &self,
        selector: impl std::fmt::Display,
        other: &ScoreVar<U>,
        other_selector: impl std::fmt::Display,
    ) -> String {
        format!(
            "scoreboard players operation {} {} < {} {}",
            selector,
            self.objective_name(),
            other_selector,
            other.objective_name()
        )
    }

    /// Set this score to the maximum of itself and another variable.
    ///
    /// Generated command:
    /// ```text
    /// scoreboard players operation <sel> <self_obj> > <other_sel> <other_obj>
    /// ```
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreVar::max_op",
        aliases = ["sand::prelude::ScoreVar::max_op"],
        module = "sand::state",
        kind = "method",
        summary = "Set this score to the maximum of itself and another variable.",
        context = "Set this score to the maximum of itself and another variable. Generated command:",
        minecraft = "Generated command:",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(selector = "`selector` provides the Minecraft target selection used to set this score to the maximum of itself and another variable.", other = "`other` supplies the other value used to set this score to the maximum of itself and another variable.", other_selector = "`other_selector` supplies the other selector value used to set this score to the maximum of itself and another variable."),
        returns = "The string value produced to set this score to the maximum of itself and another variable.",
        example = "use std::fmt;\nuse sand::prelude::*;\n\nfn demonstrate<T: 'static, U: 'static>(score_var_value: &sand::state::ScoreVar < T >, selector: impl std::fmt::Display, other: & sand::state::ScoreVar < U >, other_selector: impl std::fmt::Display)  {\n    let max_op = score_var_value.max_op::<U>(selector, other, other_selector);\n}",
    )]
    pub fn max_op<U>(
        &self,
        selector: impl std::fmt::Display,
        other: &ScoreVar<U>,
        other_selector: impl std::fmt::Display,
    ) -> String {
        format!(
            "scoreboard players operation {} {} > {} {}",
            selector,
            self.objective_name(),
            other_selector,
            other.objective_name()
        )
    }

    /// Condition: score equals zero.
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreVar::is_zero",
        aliases = ["sand::prelude::ScoreVar::is_zero"],
        module = "sand::state",
        kind = "method",
        summary = "Condition: score equals zero.",
        context = "Condition: score equals zero. This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(selector = "`selector` provides the Minecraft target selection used to condition score equals zero."),
        returns = "The `sand :: condition :: Condition` value produced to condition score equals zero.",
        example = "use sand::prelude::*;\n\nfn demonstrate<T: 'static>(score_var_value: &sand::state::ScoreVar < T >, selector: & str)  {\n    let is_zero = score_var_value.is_zero(selector);\n}",
    )]
    pub fn is_zero(&self, selector: &str) -> crate::condition::Condition {
        self.of(selector).is_zero()
    }

    /// Condition: score is not zero.
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreVar::is_nonzero",
        aliases = ["sand::prelude::ScoreVar::is_nonzero"],
        module = "sand::state",
        kind = "method",
        summary = "Condition: score is not zero.",
        context = "Condition: score is not zero. This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(selector = "`selector` provides the Minecraft target selection used to condition score is not zero."),
        returns = "The `sand :: condition :: Condition` value produced to condition score is not zero.",
        example = "use sand::prelude::*;\n\nfn demonstrate<T: 'static>(score_var_value: &sand::state::ScoreVar < T >, selector: & str)  {\n    let is_nonzero = score_var_value.is_nonzero(selector);\n}",
    )]
    pub fn is_nonzero(&self, selector: &str) -> crate::condition::Condition {
        self.of(selector).is_nonzero()
    }

    /// Condition: score is strictly positive (> 0).
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreVar::positive",
        aliases = ["sand::prelude::ScoreVar::positive"],
        module = "sand::state",
        kind = "method",
        summary = "Condition: score is strictly positive (> 0).",
        context = "Condition: score is strictly positive (> 0). This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(selector = "`selector` provides the Minecraft target selection used to condition score is strictly positive (> 0)."),
        returns = "The `sand :: condition :: Condition` value produced to condition score is strictly positive (> 0).",
        example = "use sand::prelude::*;\n\nfn demonstrate<T: 'static>(score_var_value: &sand::state::ScoreVar < T >, selector: & str)  {\n    let positive = score_var_value.positive(selector);\n}",
    )]
    pub fn positive(&self, selector: &str) -> crate::condition::Condition {
        self.of(selector).positive()
    }

    /// Condition: score is strictly negative (< 0).
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreVar::negative",
        aliases = ["sand::prelude::ScoreVar::negative"],
        module = "sand::state",
        kind = "method",
        summary = "Condition: score is strictly negative (< 0).",
        context = "Condition: score is strictly negative (< 0). This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(selector = "`selector` provides the Minecraft target selection used to condition score is strictly negative (< 0)."),
        returns = "The `sand :: condition :: Condition` value produced to condition score is strictly negative (< 0).",
        example = "use sand::prelude::*;\n\nfn demonstrate<T: 'static>(score_var_value: &sand::state::ScoreVar < T >, selector: & str)  {\n    let negative = score_var_value.negative(selector);\n}",
    )]
    pub fn negative(&self, selector: &str) -> crate::condition::Condition {
        self.of(selector).negative()
    }
}

// ── ScoreRef ──────────────────────────────────────────────────────────────────

#[sand_macros::api(
    registry = sand_api_contract,
    path = "sand::state::ScoreRef",
    aliases = ["sand::prelude::ScoreRef"],
    module = "sand::state",
    summary = "A [`ScoreVar`] bound to a selector — used to build [`Condition`]s.",
    context = "A [`ScoreVar`] bound to a selector — used to build [`Condition`]s. Produced by [`ScoreVar::of`].",
    minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
    use_when = ["Working with typed gameplay state or composing state transitions"],
    avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
    example = "use sand::state::ScoreRef;",
)]
/// A [`ScoreVar`] bound to a selector — used to build [`Condition`]s.
///
/// Produced by [`ScoreVar::of`].
pub struct ScoreRef<'a, T = i32> {
    /// The underlying `ScoreVar` name (used to derive the objective name).
    objective: &'a str,
    selector: String,
    _marker: PhantomData<T>,
}

impl<'a, T> ScoreRef<'a, T> {
    fn obj(&self) -> String {
        objective_name(self.objective)
    }

    /// `execute store result storage <field> int 1 run scoreboard players get
    /// <selector> <obj>` — copy this score into a typed storage field.
    ///
    /// The typed counterpart to reading storage into a score
    /// ([`StorageField::set`] combined with [`ScoreVar::set`]/`.add`); use
    /// this whenever a handler needs to snapshot a scoreboard value into NBT
    /// storage (e.g. for machine-readable evidence) without hand-writing an
    /// `execute store result storage ...` command.
    ///
    /// ```
    /// use sand_core::state::{ScoreVar, StorageSchema};
    ///
    /// static SEQ: ScoreVar = ScoreVar::new("seq");
    /// static AUDIT: StorageSchema<()> = StorageSchema::new("pack:audit", "audit");
    ///
    /// let cmd = SEQ.of("@s").store_into(AUDIT.field::<i32>("sequence"));
    /// assert_eq!(
    ///     cmd,
    ///     "execute store result storage pack:audit audit.sequence int 1 run scoreboard players get @s seq"
    /// );
    /// ```
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreRef::store_into",
        aliases = ["sand::prelude::ScoreRef::store_into"],
        module = "sand::state",
        kind = "method",
        summary = "`execute store result storage <field> int 1 run scoreboard players get <selector> <obj>` — copy this score into a typed storage field.",
        context = "`execute store result storage <field> int 1 run scoreboard players get <selector> <obj>` — copy this score into a typed storage field. The typed counterpart to reading storage into a score ([`StorageField::set`] combined with [`ScoreVar::set`]/`.add`); use this whenever a handler needs to snapshot a scoreboard value into NBT storage (e.g. for machine-readable evidence) without hand-writing an `execute store result storage ...` command.",
        minecraft = "The typed counterpart to reading storage into a score ([`StorageField::set`] combined with [`ScoreVar::set`]/`.add`); use this whenever a handler needs to snapshot a scoreboard value into NBT storage (e.g. for machine-readable evidence) without hand-writing an `execute store result storage ...` command.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(field = "`field` supplies the field value used to emit the documented `execute store result storage <field> int 1 run scoreboard players get <selector> <obj>` — copy this score into a typed storage field form."),
        returns = "The string value produced to emit the documented `execute store result storage <field> int 1 run scoreboard players get <selector> <obj>` — copy this score into a typed storage field form.",
        example = "use {sand::state::ScoreVar, sand::data::StorageSchema};\nstatic SEQ: ScoreVar = ScoreVar::new(\"seq\");\nstatic AUDIT: StorageSchema<()> = StorageSchema::new(\"pack:audit\", \"audit\");\nlet cmd = SEQ.of(\"@s\").store_into(AUDIT.field::<i32>(\"sequence\"));\nassert_eq!(\ncmd,\n\"execute store result storage pack:audit audit.sequence int 1 run scoreboard players get @s seq\"\n);",
    )]
    pub fn store_into<Schema, U>(&self, field: StorageField<Schema, U>) -> String {
        format!(
            "execute store result storage {} {} int 1 run scoreboard players get {} {}",
            field.storage(),
            field.full_path(),
            self.selector,
            self.obj()
        )
    }

    /// Return the typed scoreboard entry represented by this reference.
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreRef::operand",
        aliases = ["sand::prelude::ScoreRef::operand"],
        module = "sand::state",
        kind = "method",
        summary = "Return the typed scoreboard entry represented by this reference.",
        context = "Return the typed scoreboard entry represented by this reference. This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        returns = "Return the typed scoreboard entry represented by this reference.",
        example = "use sand::prelude::*;\n\nfn demonstrate<'a, T: 'static>(score_ref_value: &sand::state::ScoreRef < 'a , T >)  {\n    let operand = score_ref_value.operand();\n}",
    )]
    pub fn operand(&self) -> ScoreOperand {
        ScoreOperand {
            selector: self.selector.clone(),
            objective: self.obj(),
        }
    }

    fn operation<O: Into<ScoreOperand>>(self, op: ScoreOperation, other: O) -> String {
        let left = self.operand();
        let right = other.into();
        score_operation_command(&left, op, &right)
    }

    /// Assign this score from another score entry (`=`).
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreRef::assign",
        aliases = ["sand::prelude::ScoreRef::assign"],
        module = "sand::state",
        kind = "method",
        summary = "Assign this score from another score entry (`=`).",
        context = "Assign this score from another score entry (`=`). This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(other = "`other` supplies the other value used to assign this score from another score entry (`=`)."),
        returns = "The string value produced to assign this score from another score entry (`=`).",
        example = "use sand::prelude::*;\n\nfn demonstrate<'a, T: 'static, O : Into < sand::state::ScoreOperand > + 'static>(score_ref_value: sand::state::ScoreRef < 'a , T >, other: O)  {\n    let assign = score_ref_value.assign::<O>(other);\n}",
    )]
    pub fn assign<O: Into<ScoreOperand>>(self, other: O) -> String {
        self.operation(ScoreOperation::Assign, other)
    }

    /// Add another score entry (`+=`).
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreRef::add_score",
        aliases = ["sand::prelude::ScoreRef::add_score"],
        module = "sand::state",
        kind = "method",
        summary = "Add another score entry (`+=`).",
        context = "Add another score entry (`+=`). This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(other = "`other` supplies the other value used to add another score entry (`+=`)."),
        returns = "The string value produced to add another score entry (`+=`).",
        example = "use sand::prelude::*;\n\nfn demonstrate<'a, T: 'static, O : Into < sand::state::ScoreOperand > + 'static>(score_ref_value: sand::state::ScoreRef < 'a , T >, other: O)  {\n    let add_score = score_ref_value.add_score::<O>(other);\n}",
    )]
    pub fn add_score<O: Into<ScoreOperand>>(self, other: O) -> String {
        self.operation(ScoreOperation::Add, other)
    }

    /// Subtract another score entry (`-=`).
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreRef::sub_score",
        aliases = ["sand::prelude::ScoreRef::sub_score"],
        module = "sand::state",
        kind = "method",
        summary = "Subtract another score entry (`-=`).",
        context = "Subtract another score entry (`-=`). This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(other = "`other` supplies the other value used to subtract another score entry (`-=`)."),
        returns = "The string value produced to subtract another score entry (`-=`).",
        example = "use sand::prelude::*;\n\nfn demonstrate<'a, T: 'static, O : Into < sand::state::ScoreOperand > + 'static>(score_ref_value: sand::state::ScoreRef < 'a , T >, other: O)  {\n    let sub_score = score_ref_value.sub_score::<O>(other);\n}",
    )]
    pub fn sub_score<O: Into<ScoreOperand>>(self, other: O) -> String {
        self.operation(ScoreOperation::Sub, other)
    }

    /// Multiply by another score entry (`*=`).
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreRef::mul_score",
        aliases = ["sand::prelude::ScoreRef::mul_score"],
        module = "sand::state",
        kind = "method",
        summary = "Multiply by another score entry (`*=`).",
        context = "Multiply by another score entry (`*=`). This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(other = "`other` supplies the other value used to multiply by another score entry (`*=`)."),
        returns = "The string value produced to multiply by another score entry (`*=`).",
        example = "use sand::prelude::*;\n\nfn demonstrate<'a, T: 'static, O : Into < sand::state::ScoreOperand > + 'static>(score_ref_value: sand::state::ScoreRef < 'a , T >, other: O)  {\n    let mul_score = score_ref_value.mul_score::<O>(other);\n}",
    )]
    pub fn mul_score<O: Into<ScoreOperand>>(self, other: O) -> String {
        self.operation(ScoreOperation::Mul, other)
    }

    /// Divide by another score entry (`/=`). Scoreboard math is integer-only;
    /// division by zero remains a vanilla runtime error.
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreRef::div_score",
        aliases = ["sand::prelude::ScoreRef::div_score"],
        module = "sand::state",
        kind = "method",
        summary = "Divide by another score entry (`/=`). Scoreboard math is integer-only; division by zero remains a vanilla runtime error.",
        context = "Divide by another score entry (`/=`). Scoreboard math is integer-only; division by zero remains a vanilla runtime error. This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(other = "`other` supplies the other value used to divide by another score entry (`/=`). Scoreboard math is integer-only; division by zero remains a vanilla runtime error."),
        returns = "The string value produced to divide by another score entry (`/=`). Scoreboard math is integer-only; division by zero remains a vanilla runtime error.",
        example = "use sand::prelude::*;\n\nfn demonstrate<'a, T: 'static, O : Into < sand::state::ScoreOperand > + 'static>(score_ref_value: sand::state::ScoreRef < 'a , T >, other: O)  {\n    let div_score = score_ref_value.div_score::<O>(other);\n}",
    )]
    pub fn div_score<O: Into<ScoreOperand>>(self, other: O) -> String {
        self.operation(ScoreOperation::Div, other)
    }

    /// Modulo another score entry (`%=`). Modulo by zero remains a vanilla
    /// runtime error.
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreRef::mod_score",
        aliases = ["sand::prelude::ScoreRef::mod_score"],
        module = "sand::state",
        kind = "method",
        summary = "Modulo another score entry (`%=`). Modulo by zero remains a vanilla runtime error.",
        context = "Modulo another score entry (`%=`). Modulo by zero remains a vanilla runtime error. This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(other = "`other` supplies the other value used to modulo another score entry (`%=`). Modulo by zero remains a vanilla runtime error."),
        returns = "The string value produced to modulo another score entry (`%=`). Modulo by zero remains a vanilla runtime error.",
        example = "use sand::prelude::*;\n\nfn demonstrate<'a, T: 'static, O : Into < sand::state::ScoreOperand > + 'static>(score_ref_value: sand::state::ScoreRef < 'a , T >, other: O)  {\n    let mod_score = score_ref_value.mod_score::<O>(other);\n}",
    )]
    pub fn mod_score<O: Into<ScoreOperand>>(self, other: O) -> String {
        self.operation(ScoreOperation::Mod, other)
    }

    /// Keep the minimum of this and another score entry (`<`).
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreRef::min_score",
        aliases = ["sand::prelude::ScoreRef::min_score"],
        module = "sand::state",
        kind = "method",
        summary = "Keep the minimum of this and another score entry (`<`).",
        context = "Keep the minimum of this and another score entry (`<`). This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(other = "`other` supplies the other value used to keep the minimum of this and another score entry (`<`)."),
        returns = "The string value produced to keep the minimum of this and another score entry (`<`).",
        example = "use sand::prelude::*;\n\nfn demonstrate<'a, T: 'static, O : Into < sand::state::ScoreOperand > + 'static>(score_ref_value: sand::state::ScoreRef < 'a , T >, other: O)  {\n    let min_score = score_ref_value.min_score::<O>(other);\n}",
    )]
    pub fn min_score<O: Into<ScoreOperand>>(self, other: O) -> String {
        self.operation(ScoreOperation::Min, other)
    }

    /// Keep the maximum of this and another score entry (`>`).
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreRef::max_score",
        aliases = ["sand::prelude::ScoreRef::max_score"],
        module = "sand::state",
        kind = "method",
        summary = "Keep the maximum of this and another score entry (`>`).",
        context = "Keep the maximum of this and another score entry (`>`). This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(other = "`other` supplies the other value used to keep the maximum of this and another score entry (`>`)."),
        returns = "The string value produced to keep the maximum of this and another score entry (`>`).",
        example = "use sand::prelude::*;\n\nfn demonstrate<'a, T: 'static, O : Into < sand::state::ScoreOperand > + 'static>(score_ref_value: sand::state::ScoreRef < 'a , T >, other: O)  {\n    let max_score = score_ref_value.max_score::<O>(other);\n}",
    )]
    pub fn max_score<O: Into<ScoreOperand>>(self, other: O) -> String {
        self.operation(ScoreOperation::Max, other)
    }

    /// Swap this score entry with another (`><`).
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreRef::swap",
        aliases = ["sand::prelude::ScoreRef::swap"],
        module = "sand::state",
        kind = "method",
        summary = "Swap this score entry with another (`><`).",
        context = "Swap this score entry with another (`><`). This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(other = "`other` supplies the other value used to swap this score entry with another (`><`)."),
        returns = "The string value produced to swap this score entry with another (`><`).",
        example = "use sand::prelude::*;\n\nfn demonstrate<'a, T: 'static, O : Into < sand::state::ScoreOperand > + 'static>(score_ref_value: sand::state::ScoreRef < 'a , T >, other: O)  {\n    let swap = score_ref_value.swap::<O>(other);\n}",
    )]
    pub fn swap<O: Into<ScoreOperand>>(self, other: O) -> String {
        self.operation(ScoreOperation::Swap, other)
    }

    /// Set this score to `value * percent / 100`.
    ///
    /// Scoreboard math is integer-only, so the final division truncates toward
    /// zero. Sand registers the generated percentage constants automatically.
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreRef::set_percent",
        aliases = ["sand::prelude::ScoreRef::set_percent"],
        module = "sand::state",
        kind = "method",
        summary = "Set this score to `value * percent / 100`. Scoreboard math is integer-only, so the final division truncates toward zero. Sand registers the generated percentage constants automatically.",
        context = "Set this score to `value * percent / 100`. Scoreboard math is integer-only, so the final division truncates toward zero. Sand registers the generated percentage constants automatically. This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Scoreboard math is integer-only, so the final division truncates toward zero. Sand registers the generated percentage constants automatically.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(value = "`value` provides the value being applied or compared used to set this score to `value * percent / 100`. Scoreboard math is integer-only, so the final division truncates toward zero. Sand registers the generated percentage constants automatically.", percent = "`percent` supplies the percent value used to set this score to `value * percent / 100`. Scoreboard math is integer-only, so the final division truncates toward zero. Sand registers the generated percentage constants automatically."),
        returns = "The ordered values produced to set this score to `value * percent / 100`. Scoreboard math is integer-only, so the final division truncates toward zero. Sand registers the generated percentage constants automatically.",
        example = "use sand::prelude::*;\n\nfn demonstrate<'a, T: 'static, O : Into < sand::state::ScoreOperand > + 'static>(score_ref_value: sand::state::ScoreRef < 'a , T >, value: O, percent: i32)  {\n    let values = score_ref_value.set_percent::<O>(value, percent);\n}",
    )]
    pub fn set_percent<O: Into<ScoreOperand>>(self, value: O, percent: i32) -> Vec<String> {
        let target = self.operand();
        let value = value.into();
        let percent = score_constant_operand("score_percent", percent);
        let hundred = score_constant_operand("score_percent_denominator", 100);
        vec![
            score_operation_command(&target, ScoreOperation::Assign, &value),
            score_operation_command(&target, ScoreOperation::Mul, &percent),
            score_operation_command(&target, ScoreOperation::Div, &hundred),
        ]
    }

    /// Scale this score in place by `percent / 100`.
    ///
    /// Scoreboard math is integer-only, so the final division truncates toward
    /// zero. Use [`ScoreRef::set_percent`] when the source and destination are
    /// different scores.
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreRef::scale_percent",
        aliases = ["sand::prelude::ScoreRef::scale_percent"],
        module = "sand::state",
        kind = "method",
        summary = "Scale this score in place by `percent / 100`. Scoreboard math is integer-only, so the final division truncates toward zero. Use [`ScoreRef::set_percent`] when the source and destination are different scores.",
        context = "Scale this score in place by `percent / 100`. Scoreboard math is integer-only, so the final division truncates toward zero. Use [`ScoreRef::set_percent`] when the source and destination are different scores. This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Scoreboard math is integer-only, so the final division truncates toward zero. Use [`ScoreRef::set_percent`] when the source and destination are different scores.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(percent = "`percent` supplies the percent value used to scale this score in place by `percent / 100`. Scoreboard math is integer-only, so the final division truncates toward zero. Use [`ScoreRef::set_percent`] when the source and destination are different scores."),
        returns = "The ordered values produced to scale this score in place by `percent / 100`. Scoreboard math is integer-only, so the final division truncates toward zero. Use [`ScoreRef::set_percent`] when the source and destination are different scores.",
        example = "use sand::prelude::*;\n\nfn demonstrate<'a, T: 'static>(score_ref_value: sand::state::ScoreRef < 'a , T >, percent: i32)  {\n    let values = score_ref_value.scale_percent(percent);\n}",
    )]
    pub fn scale_percent(self, percent: i32) -> Vec<String> {
        let target = self.operand();
        let percent = score_constant_operand("score_percent", percent);
        let hundred = score_constant_operand("score_percent_denominator", 100);
        vec![
            score_operation_command(&target, ScoreOperation::Mul, &percent),
            score_operation_command(&target, ScoreOperation::Div, &hundred),
        ]
    }

    /// Set this score to `current * scale / max`.
    ///
    /// This emits the direct vanilla operations and does not hide division by
    /// zero. Use [`ScoreRef::safe_divide`] when `max` may be zero.
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreRef::set_ratio",
        aliases = ["sand::prelude::ScoreRef::set_ratio"],
        module = "sand::state",
        kind = "method",
        summary = "Set this score to `current * scale / max`. This emits the direct vanilla operations and does not hide division by zero. Use [`ScoreRef::safe_divide`] when `max` may be zero.",
        context = "Set this score to `current * scale / max`. This emits the direct vanilla operations and does not hide division by zero. Use [`ScoreRef::safe_divide`] when `max` may be zero. This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(current = "`current` supplies the current value used to set this score to `current * scale / max`. This emits the direct vanilla operations and does not hide division by zero. Use [`ScoreRef::safe_divide`] when `max` may be zero.", max = "This emits the direct vanilla operations and does not hide division by zero. Use [`ScoreRef::safe_divide`] when `max` may be zero.", scale = "`scale` supplies the scale value used to set this score to `current * scale / max`. This emits the direct vanilla operations and does not hide division by zero. Use [`ScoreRef::safe_divide`] when `max` may be zero."),
        returns = "The ordered values produced to set this score to `current * scale / max`. This emits the direct vanilla operations and does not hide division by zero. Use [`ScoreRef::safe_divide`] when `max` may be zero.",
        example = "use sand::prelude::*;\n\nfn demonstrate<'a, T: 'static, N : Into < sand::state::ScoreOperand > + 'static, D : Into < sand::state::ScoreOperand > + 'static>(score_ref_value: sand::state::ScoreRef < 'a , T >, current: N, max: D, scale: i32)  {\n    let values = score_ref_value.set_ratio::<N, D>(current, max, scale);\n}",
    )]
    pub fn set_ratio<N: Into<ScoreOperand>, D: Into<ScoreOperand>>(
        self,
        current: N,
        max: D,
        scale: i32,
    ) -> Vec<String> {
        let target = self.operand();
        let current = current.into();
        let max = max.into();
        let scale = score_constant_operand("score_ratio_scale", scale);
        vec![
            score_operation_command(&target, ScoreOperation::Assign, &current),
            score_operation_command(&target, ScoreOperation::Mul, &scale),
            score_operation_command(&target, ScoreOperation::Div, &max),
        ]
    }

    /// Divide this score by another score only when the divisor is non-zero.
    ///
    /// If the divisor is zero, the target is set to `fallback` instead. This
    /// keeps generated output explicit about the branch that avoids vanilla's
    /// division-by-zero runtime failure.
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreRef::safe_divide",
        aliases = ["sand::prelude::ScoreRef::safe_divide"],
        module = "sand::state",
        kind = "method",
        summary = "Divide this score by another score only when the divisor is non-zero.",
        context = "Divide this score by another score only when the divisor is non-zero. If the divisor is zero, the target is set to `fallback` instead. This keeps generated output explicit about the branch that avoids vanilla's division-by-zero runtime failure.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(divisor = "`divisor` supplies the divisor value used to divide this score by another score only when the divisor is non-zero.", fallback = "If the divisor is zero, the target is set to `fallback` instead. This keeps generated output explicit about the branch that avoids vanilla's division-by-zero runtime failure."),
        returns = "The ordered values produced to divide this score by another score only when the divisor is non-zero.",
        example = "use sand::prelude::*;\n\nfn demonstrate<'a, T: 'static, O : Into < sand::state::ScoreOperand > + 'static>(score_ref_value: sand::state::ScoreRef < 'a , T >, divisor: O, fallback: i32)  {\n    let values = score_ref_value.safe_divide::<O>(divisor, fallback);\n}",
    )]
    pub fn safe_divide<O: Into<ScoreOperand>>(self, divisor: O, fallback: i32) -> Vec<String> {
        let target = self.operand();
        let divisor = divisor.into();
        vec![
            format!(
                "execute unless score {} {} matches 0 run {}",
                divisor.selector,
                divisor.objective,
                score_operation_command(&target, ScoreOperation::Div, &divisor)
            ),
            format!(
                "execute if score {} {} matches 0 run {}",
                divisor.selector,
                divisor.objective,
                score_set_command(&target, fallback)
            ),
        ]
    }

    /// Clamp this score between two other score entries.
    ///
    /// The first command enforces the lower bound with vanilla's `>` operation
    /// (`max`), and the second enforces the upper bound with `<` (`min`).
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreRef::clamp_score",
        aliases = ["sand::prelude::ScoreRef::clamp_score"],
        module = "sand::state",
        kind = "method",
        summary = "Clamp this score between two other score entries.",
        context = "Clamp this score between two other score entries. The first command enforces the lower bound with vanilla's `>` operation (`max`), and the second enforces the upper bound with `<` (`min`).",
        minecraft = "The first command enforces the lower bound with vanilla's `>` operation (`max`), and the second enforces the upper bound with `<` (`min`).",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(min = "The first command enforces the lower bound with vanilla's `>` operation (`max`), and the second enforces the upper bound with `<` (`min`).", max = "The first command enforces the lower bound with vanilla's `>` operation (`max`), and the second enforces the upper bound with `<` (`min`)."),
        returns = "The ordered values produced to clamp this score between two other score entries.",
        example = "use sand::prelude::*;\n\nfn demonstrate<'a, T: 'static, L : Into < sand::state::ScoreOperand > + 'static, U : Into < sand::state::ScoreOperand > + 'static>(score_ref_value: sand::state::ScoreRef < 'a , T >, min: L, max: U)  {\n    let values = score_ref_value.clamp_score::<L, U>(min, max);\n}",
    )]
    pub fn clamp_score<L: Into<ScoreOperand>, U: Into<ScoreOperand>>(
        self,
        min: L,
        max: U,
    ) -> Vec<String> {
        let target = self.operand();
        let min = min.into();
        let max = max.into();
        vec![
            score_operation_command(&target, ScoreOperation::Max, &min),
            score_operation_command(&target, ScoreOperation::Min, &max),
        ]
    }

    /// Add `amount` and then clamp to the literal `[min, max]` range.
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreRef::saturating_add",
        aliases = ["sand::prelude::ScoreRef::saturating_add"],
        module = "sand::state",
        kind = "method",
        summary = "Add `amount` and then clamp to the literal `[min, max]` range.",
        context = "Add `amount` and then clamp to the literal `[min, max]` range. This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(amount = "Add `amount` and then clamp to the literal `[min, max]` range.", min = "`min` provides the inclusive lower bound used to add `amount` and then clamp to the literal `[min, max]` range.", max = "`max` provides the inclusive upper bound used to add `amount` and then clamp to the literal `[min, max]` range."),
        returns = "The ordered values produced to add `amount` and then clamp to the literal `[min, max]` range.",
        example = "use sand::prelude::*;\n\nfn demonstrate<'a, T: 'static>(score_ref_value: sand::state::ScoreRef < 'a , T >, amount: i32, min: i32, max: i32)  {\n    let values = score_ref_value.saturating_add(amount, min, max);\n}",
    )]
    pub fn saturating_add(self, amount: i32, min: i32, max: i32) -> Vec<String> {
        let target = self.operand();
        vec![
            format!(
                "scoreboard players add {} {} {amount}",
                target.selector, target.objective
            ),
            format!(
                "execute if score {} {} matches ..{} run {}",
                target.selector,
                target.objective,
                min.saturating_sub(1),
                score_set_command(&target, min)
            ),
            format!(
                "execute if score {} {} matches {}.. run {}",
                target.selector,
                target.objective,
                max.saturating_add(1),
                score_set_command(&target, max)
            ),
        ]
    }

    /// Subtract `amount` and then clamp to the literal `[min, max]` range.
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreRef::saturating_sub",
        aliases = ["sand::prelude::ScoreRef::saturating_sub"],
        module = "sand::state",
        kind = "method",
        summary = "Subtract `amount` and then clamp to the literal `[min, max]` range.",
        context = "Subtract `amount` and then clamp to the literal `[min, max]` range. This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(amount = "Subtract `amount` and then clamp to the literal `[min, max]` range.", min = "`min` provides the inclusive lower bound used to subtract `amount` and then clamp to the literal `[min, max]` range.", max = "`max` provides the inclusive upper bound used to subtract `amount` and then clamp to the literal `[min, max]` range."),
        returns = "The ordered values produced to subtract `amount` and then clamp to the literal `[min, max]` range.",
        example = "use sand::prelude::*;\n\nfn demonstrate<'a, T: 'static>(score_ref_value: sand::state::ScoreRef < 'a , T >, amount: i32, min: i32, max: i32)  {\n    let values = score_ref_value.saturating_sub(amount, min, max);\n}",
    )]
    pub fn saturating_sub(self, amount: i32, min: i32, max: i32) -> Vec<String> {
        let target = self.operand();
        vec![
            format!(
                "scoreboard players remove {} {} {amount}",
                target.selector, target.objective
            ),
            format!(
                "execute if score {} {} matches ..{} run {}",
                target.selector,
                target.objective,
                min.saturating_sub(1),
                score_set_command(&target, min)
            ),
            format!(
                "execute if score {} {} matches {}.. run {}",
                target.selector,
                target.objective,
                max.saturating_add(1),
                score_set_command(&target, max)
            ),
        ]
    }

    fn compare<O: Into<ScoreOperand>>(self, op: ScoreCompareOp, other: O) -> Condition {
        Condition::score_compare(self.operand(), op, other.into())
    }

    /// Compare this score to another score entry (`=`).
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreRef::eq_score",
        aliases = ["sand::prelude::ScoreRef::eq_score"],
        module = "sand::state",
        kind = "method",
        summary = "Compare this score to another score entry (`=`).",
        context = "Compare this score to another score entry (`=`). This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(other = "`other` supplies the other value used to compare this score to another score entry (`=`)."),
        returns = "The `Condition` value produced to compare this score to another score entry (`=`).",
        example = "use sand::prelude::*;\n\nfn demonstrate<'a, T: 'static, O : Into < sand::state::ScoreOperand > + 'static>(score_ref_value: sand::state::ScoreRef < 'a , T >, other: O)  {\n    let eq_score = score_ref_value.eq_score::<O>(other);\n}",
    )]
    pub fn eq_score<O: Into<ScoreOperand>>(self, other: O) -> Condition {
        self.compare(ScoreCompareOp::Eq, other)
    }

    /// Compare this score as not equal to another score entry.
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreRef::ne_score",
        aliases = ["sand::prelude::ScoreRef::ne_score"],
        module = "sand::state",
        kind = "method",
        summary = "Compare this score as not equal to another score entry.",
        context = "Compare this score as not equal to another score entry. This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(other = "`other` supplies the other value used to compare this score as not equal to another score entry."),
        returns = "The `Condition` value produced to compare this score as not equal to another score entry.",
        example = "use sand::prelude::*;\n\nfn demonstrate<'a, T: 'static, O : Into < sand::state::ScoreOperand > + 'static>(score_ref_value: sand::state::ScoreRef < 'a , T >, other: O)  {\n    let ne_score = score_ref_value.ne_score::<O>(other);\n}",
    )]
    pub fn ne_score<O: Into<ScoreOperand>>(self, other: O) -> Condition {
        !self.eq_score(other)
    }

    /// Compare this score as greater than another score entry (`>`).
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreRef::gt_score",
        aliases = ["sand::prelude::ScoreRef::gt_score"],
        module = "sand::state",
        kind = "method",
        summary = "Compare this score as greater than another score entry (`>`).",
        context = "Compare this score as greater than another score entry (`>`). This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(other = "`other` supplies the other value used to compare this score as greater than another score entry (`>`)."),
        returns = "The `Condition` value produced to compare this score as greater than another score entry (`>`).",
        example = "use sand::prelude::*;\n\nfn demonstrate<'a, T: 'static, O : Into < sand::state::ScoreOperand > + 'static>(score_ref_value: sand::state::ScoreRef < 'a , T >, other: O)  {\n    let gt_score = score_ref_value.gt_score::<O>(other);\n}",
    )]
    pub fn gt_score<O: Into<ScoreOperand>>(self, other: O) -> Condition {
        self.compare(ScoreCompareOp::Gt, other)
    }

    /// Compare this score as greater than or equal to another score entry (`>=`).
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreRef::gte_score",
        aliases = ["sand::prelude::ScoreRef::gte_score"],
        module = "sand::state",
        kind = "method",
        summary = "Compare this score as greater than or equal to another score entry (`>=`).",
        context = "Compare this score as greater than or equal to another score entry (`>=`). This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(other = "`other` supplies the other value used to compare this score as greater than or equal to another score entry (`>=`)."),
        returns = "The `Condition` value produced to compare this score as greater than or equal to another score entry (`>=`).",
        example = "use sand::prelude::*;\n\nfn demonstrate<'a, T: 'static, O : Into < sand::state::ScoreOperand > + 'static>(score_ref_value: sand::state::ScoreRef < 'a , T >, other: O)  {\n    let gte_score = score_ref_value.gte_score::<O>(other);\n}",
    )]
    pub fn gte_score<O: Into<ScoreOperand>>(self, other: O) -> Condition {
        self.compare(ScoreCompareOp::Gte, other)
    }

    /// Compare this score as less than another score entry (`<`).
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreRef::lt_score",
        aliases = ["sand::prelude::ScoreRef::lt_score"],
        module = "sand::state",
        kind = "method",
        summary = "Compare this score as less than another score entry (`<`).",
        context = "Compare this score as less than another score entry (`<`). This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(other = "`other` supplies the other value used to compare this score as less than another score entry (`<`)."),
        returns = "The `Condition` value produced to compare this score as less than another score entry (`<`).",
        example = "use sand::prelude::*;\n\nfn demonstrate<'a, T: 'static, O : Into < sand::state::ScoreOperand > + 'static>(score_ref_value: sand::state::ScoreRef < 'a , T >, other: O)  {\n    let lt_score = score_ref_value.lt_score::<O>(other);\n}",
    )]
    pub fn lt_score<O: Into<ScoreOperand>>(self, other: O) -> Condition {
        self.compare(ScoreCompareOp::Lt, other)
    }

    /// Compare this score as less than or equal to another score entry (`<=`).
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreRef::lte_score",
        aliases = ["sand::prelude::ScoreRef::lte_score"],
        module = "sand::state",
        kind = "method",
        summary = "Compare this score as less than or equal to another score entry (`<=`).",
        context = "Compare this score as less than or equal to another score entry (`<=`). This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(other = "`other` supplies the other value used to compare this score as less than or equal to another score entry (`<=`)."),
        returns = "The `Condition` value produced to compare this score as less than or equal to another score entry (`<=`).",
        example = "use sand::prelude::*;\n\nfn demonstrate<'a, T: 'static, O : Into < sand::state::ScoreOperand > + 'static>(score_ref_value: sand::state::ScoreRef < 'a , T >, other: O)  {\n    let lte_score = score_ref_value.lte_score::<O>(other);\n}",
    )]
    pub fn lte_score<O: Into<ScoreOperand>>(self, other: O) -> Condition {
        self.compare(ScoreCompareOp::Lte, other)
    }

    /// Begin an integer-only score expression. Its setup commands are emitted
    /// before the final branch condition is evaluated.
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreRef::expr",
        aliases = ["sand::prelude::ScoreRef::expr"],
        module = "sand::state",
        kind = "method",
        summary = "Begin an integer-only score expression. Its setup commands are emitted before the final branch condition is evaluated.",
        context = "Begin an integer-only score expression. Its setup commands are emitted before the final branch condition is evaluated. This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        returns = "The `ScoreExpr < T >` value produced to begin an integer-only score expression. Its setup commands are emitted before the final branch condition is evaluated.",
        example = "use sand::prelude::*;\n\nfn demonstrate<'a, T: 'static>(score_ref_value: &sand::state::ScoreRef < 'a , T >)  {\n    let expr = score_ref_value.expr();\n}",
    )]
    pub fn expr(&self) -> ScoreExpr<T> {
        ScoreExpr {
            base: self.operand(),
            steps: Vec::new(),
            _marker: PhantomData,
        }
    }

    /// `if score <sel> <obj> matches <n>` — equal to `n`.
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreRef::eq",
        aliases = ["sand::prelude::ScoreRef::eq"],
        module = "sand::state",
        kind = "method",
        summary = "`if score <sel> <obj> matches <n>` — equal to `n`.",
        context = "`if score <sel> <obj> matches <n>` — equal to `n`. This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(n = "`if score <sel> <obj> matches <n>` — equal to `n`."),
        returns = "The `Condition` value produced to emit the documented `if score <sel> <obj> matches <n>` — equal to `n` form.",
        example = "use sand::prelude::*;\n\nfn demonstrate<'a, T: 'static>(score_ref_value: sand::state::ScoreRef < 'a , T >, n: i32)  {\n    let eq = score_ref_value.eq(n);\n}",
    )]
    pub fn eq(self, n: i32) -> Condition {
        let objective = self.obj();
        Condition::score(self.selector, objective, ScoreRange::Eq(n))
    }

    /// `unless score <sel> <obj> matches <n>` — not equal to `n`.
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreRef::ne",
        aliases = ["sand::prelude::ScoreRef::ne"],
        module = "sand::state",
        kind = "method",
        summary = "`unless score <sel> <obj> matches <n>` — not equal to `n`.",
        context = "`unless score <sel> <obj> matches <n>` — not equal to `n`. This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(n = "`unless score <sel> <obj> matches <n>` — not equal to `n`."),
        returns = "The `Condition` value produced to emit the documented `unless score <sel> <obj> matches <n>` — not equal to `n` form.",
        example = "use sand::prelude::*;\n\nfn demonstrate<'a, T: 'static>(score_ref_value: sand::state::ScoreRef < 'a , T >, n: i32)  {\n    let ne = score_ref_value.ne(n);\n}",
    )]
    pub fn ne(self, n: i32) -> Condition {
        let objective = self.obj();
        !Condition::score(self.selector, objective, ScoreRange::Eq(n))
    }

    /// `if score <sel> <obj> matches <n+1>..` — strictly greater than `n`.
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreRef::gt",
        aliases = ["sand::prelude::ScoreRef::gt"],
        module = "sand::state",
        kind = "method",
        summary = "`if score <sel> <obj> matches <n+1>..` — strictly greater than `n`.",
        context = "`if score <sel> <obj> matches <n+1>..` — strictly greater than `n`. This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(n = "`if score <sel> <obj> matches <n+1>..` — strictly greater than `n`."),
        returns = "The `Condition` value produced to emit the documented `if score <sel> <obj> matches <n+1>..` — strictly greater than `n` form.",
        example = "use sand::prelude::*;\n\nfn demonstrate<'a, T: 'static>(score_ref_value: sand::state::ScoreRef < 'a , T >, n: i32)  {\n    let gt = score_ref_value.gt(n);\n}",
    )]
    pub fn gt(self, n: i32) -> Condition {
        let objective = self.obj();
        Condition::score(self.selector, objective, ScoreRange::Gt(n))
    }

    /// `if score <sel> <obj> matches <n>..` — greater than or equal to `n`.
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreRef::gte",
        aliases = ["sand::prelude::ScoreRef::gte"],
        module = "sand::state",
        kind = "method",
        summary = "`if score <sel> <obj> matches <n>..` — greater than or equal to `n`.",
        context = "`if score <sel> <obj> matches <n>..` — greater than or equal to `n`. This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(n = "`if score <sel> <obj> matches <n>..` — greater than or equal to `n`."),
        returns = "The `Condition` value produced to emit the documented `if score <sel> <obj> matches <n>..` — greater than or equal to `n` form.",
        example = "use sand::prelude::*;\n\nfn demonstrate<'a, T: 'static>(score_ref_value: sand::state::ScoreRef < 'a , T >, n: i32)  {\n    let gte = score_ref_value.gte(n);\n}",
    )]
    pub fn gte(self, n: i32) -> Condition {
        let objective = self.obj();
        Condition::score(self.selector, objective, ScoreRange::Gte(n))
    }

    /// `if score <sel> <obj> matches ..<n-1>` — strictly less than `n`.
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreRef::lt",
        aliases = ["sand::prelude::ScoreRef::lt"],
        module = "sand::state",
        kind = "method",
        summary = "`if score <sel> <obj> matches ..<n-1>` — strictly less than `n`.",
        context = "`if score <sel> <obj> matches ..<n-1>` — strictly less than `n`. This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(n = "`if score <sel> <obj> matches ..<n-1>` — strictly less than `n`."),
        returns = "The `Condition` value produced to emit the documented `if score <sel> <obj> matches ..<n-1>` — strictly less than `n` form.",
        example = "use sand::prelude::*;\n\nfn demonstrate<'a, T: 'static>(score_ref_value: sand::state::ScoreRef < 'a , T >, n: i32)  {\n    let lt = score_ref_value.lt(n);\n}",
    )]
    pub fn lt(self, n: i32) -> Condition {
        let objective = self.obj();
        Condition::score(self.selector, objective, ScoreRange::Lt(n))
    }

    /// Condition: score equals zero.
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreRef::is_zero",
        aliases = ["sand::prelude::ScoreRef::is_zero"],
        module = "sand::state",
        kind = "method",
        summary = "Condition: score equals zero.",
        context = "Condition: score equals zero. This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        returns = "The `Condition` value produced to condition score equals zero.",
        example = "use sand::prelude::*;\n\nfn demonstrate<'a, T: 'static>(score_ref_value: sand::state::ScoreRef < 'a , T >)  {\n    let is_zero = score_ref_value.is_zero();\n}",
    )]
    pub fn is_zero(self) -> Condition {
        self.eq(0)
    }

    /// Condition: score is not zero.
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreRef::is_nonzero",
        aliases = ["sand::prelude::ScoreRef::is_nonzero"],
        module = "sand::state",
        kind = "method",
        summary = "Condition: score is not zero.",
        context = "Condition: score is not zero. This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        returns = "The `Condition` value produced to condition score is not zero.",
        example = "use sand::prelude::*;\n\nfn demonstrate<'a, T: 'static>(score_ref_value: sand::state::ScoreRef < 'a , T >)  {\n    let is_nonzero = score_ref_value.is_nonzero();\n}",
    )]
    pub fn is_nonzero(self) -> Condition {
        self.ne(0)
    }

    /// Condition: score is strictly positive (`matches 1..`).
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreRef::positive",
        aliases = ["sand::prelude::ScoreRef::positive"],
        module = "sand::state",
        kind = "method",
        summary = "Condition: score is strictly positive (`matches 1..`).",
        context = "Condition: score is strictly positive (`matches 1..`). This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        returns = "The `Condition` value produced to condition score is strictly positive (`matches 1..`).",
        example = "use sand::prelude::*;\n\nfn demonstrate<'a, T: 'static>(score_ref_value: sand::state::ScoreRef < 'a , T >)  {\n    let positive = score_ref_value.positive();\n}",
    )]
    pub fn positive(self) -> Condition {
        self.gt(0)
    }

    /// Condition: score is strictly negative (`matches ..-1`).
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreRef::negative",
        aliases = ["sand::prelude::ScoreRef::negative"],
        module = "sand::state",
        kind = "method",
        summary = "Condition: score is strictly negative (`matches ..-1`).",
        context = "Condition: score is strictly negative (`matches ..-1`). This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        returns = "The `Condition` value produced to condition score is strictly negative (`matches ..-1`).",
        example = "use sand::prelude::*;\n\nfn demonstrate<'a, T: 'static>(score_ref_value: sand::state::ScoreRef < 'a , T >)  {\n    let negative = score_ref_value.negative();\n}",
    )]
    pub fn negative(self) -> Condition {
        self.lt(0)
    }

    /// `if score <sel> <obj> matches ..<n>` — less than or equal to `n`.
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreRef::lte",
        aliases = ["sand::prelude::ScoreRef::lte"],
        module = "sand::state",
        kind = "method",
        summary = "`if score <sel> <obj> matches ..<n>` — less than or equal to `n`.",
        context = "`if score <sel> <obj> matches ..<n>` — less than or equal to `n`. This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(n = "`if score <sel> <obj> matches ..<n>` — less than or equal to `n`."),
        returns = "The `Condition` value produced to emit the documented `if score <sel> <obj> matches ..<n>` — less than or equal to `n` form.",
        example = "use sand::prelude::*;\n\nfn demonstrate<'a, T: 'static>(score_ref_value: sand::state::ScoreRef < 'a , T >, n: i32)  {\n    let lte = score_ref_value.lte(n);\n}",
    )]
    pub fn lte(self, n: i32) -> Condition {
        let objective = self.obj();
        Condition::score(self.selector, objective, ScoreRange::Lte(n))
    }

    /// `if score <sel> <obj> matches <min>..<max>` — inside an inclusive range.
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreRef::between",
        aliases = ["sand::prelude::ScoreRef::between"],
        module = "sand::state",
        kind = "method",
        summary = "`if score <sel> <obj> matches <min>..<max>` — inside an inclusive range.",
        context = "`if score <sel> <obj> matches <min>..<max>` — inside an inclusive range. This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(min = "`min` provides the inclusive lower bound used to emit the documented `if score <sel> <obj> matches <min>..<max>` — inside an inclusive range form.", max = "`max` provides the inclusive upper bound used to emit the documented `if score <sel> <obj> matches <min>..<max>` — inside an inclusive range form."),
        returns = "The `Condition` value produced to emit the documented `if score <sel> <obj> matches <min>..<max>` — inside an inclusive range form.",
        example = "use sand::prelude::*;\n\nfn demonstrate<'a, T: 'static>(score_ref_value: sand::state::ScoreRef < 'a , T >, min: i32, max: i32)  {\n    let between = score_ref_value.between(min, max);\n}",
    )]
    pub fn between(self, min: i32, max: i32) -> Condition {
        let objective = self.obj();
        Condition::score(
            self.selector,
            objective,
            ScoreRange::Between(Some(min), Some(max)),
        )
    }

    /// `unless score <sel> <obj> matches <min>..<max>` — outside an inclusive range.
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreRef::outside",
        aliases = ["sand::prelude::ScoreRef::outside"],
        module = "sand::state",
        kind = "method",
        summary = "`unless score <sel> <obj> matches <min>..<max>` — outside an inclusive range.",
        context = "`unless score <sel> <obj> matches <min>..<max>` — outside an inclusive range. This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(min = "`min` provides the inclusive lower bound used to emit the documented `unless score <sel> <obj> matches <min>..<max>` — outside an inclusive range form.", max = "`max` provides the inclusive upper bound used to emit the documented `unless score <sel> <obj> matches <min>..<max>` — outside an inclusive range form."),
        returns = "The `Condition` value produced to emit the documented `unless score <sel> <obj> matches <min>..<max>` — outside an inclusive range form.",
        example = "use sand::prelude::*;\n\nfn demonstrate<'a, T: 'static>(score_ref_value: sand::state::ScoreRef < 'a , T >, min: i32, max: i32)  {\n    let outside = score_ref_value.outside(min, max);\n}",
    )]
    pub fn outside(self, min: i32, max: i32) -> Condition {
        !self.between(min, max)
    }

    /// Validated `matches <n+1>..` — strictly greater than `n`.
    ///
    /// Rejects `n == i32::MAX`, which describes a range no `i32` score can
    /// satisfy; for example, no `i32` score can be greater than `i32::MAX`.
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreRef::try_gt",
        aliases = ["sand::prelude::ScoreRef::try_gt"],
        module = "sand::state",
        kind = "method",
        summary = "Validated `matches <n+1>..` — strictly greater than `n`.",
        context = "Validated `matches <n+1>..` — strictly greater than `n`. Rejects `n == i32::MAX`, which describes a range no `i32` score can satisfy; for example, no `i32` score can be greater than `i32::MAX`.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(n = "Validated `matches <n+1>..` — strictly greater than `n`."),
        returns = "The `sand :: command :: CommandResult < Condition >` value produced to use validated `matches <n+1>..` — strictly greater than `n`.",
        example = "use sand::prelude::*;\n\nfn demonstrate<'a, T: 'static>(score_ref_value: sand::state::ScoreRef < 'a , T >, n: i32)  {\n    let try_gt = score_ref_value.try_gt(n);\n}",
    )]
    pub fn try_gt(self, n: i32) -> sand_commands::CommandResult<Condition> {
        ScoreRange::Gt(n).validate()?;
        Ok(self.gt(n))
    }

    /// Validated `matches ..<n-1>` — strictly less than `n`.
    ///
    /// Rejects `n == i32::MIN`, which describes a range no `i32` score can
    /// satisfy.
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreRef::try_lt",
        aliases = ["sand::prelude::ScoreRef::try_lt"],
        module = "sand::state",
        kind = "method",
        summary = "Validated `matches ..<n-1>` — strictly less than `n`.",
        context = "Validated `matches ..<n-1>` — strictly less than `n`. Rejects `n == i32::MIN`, which describes a range no `i32` score can satisfy.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(n = "Validated `matches ..<n-1>` — strictly less than `n`."),
        returns = "The `sand :: command :: CommandResult < Condition >` value produced to use validated `matches ..<n-1>` — strictly less than `n`.",
        example = "use sand::prelude::*;\n\nfn demonstrate<'a, T: 'static>(score_ref_value: sand::state::ScoreRef < 'a , T >, n: i32)  {\n    let try_lt = score_ref_value.try_lt(n);\n}",
    )]
    pub fn try_lt(self, n: i32) -> sand_commands::CommandResult<Condition> {
        ScoreRange::Lt(n).validate()?;
        Ok(self.lt(n))
    }

    /// Validated inclusive range — rejects `min > max` instead of emitting an
    /// always-false `matches` fragment.
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreRef::try_between",
        aliases = ["sand::prelude::ScoreRef::try_between"],
        module = "sand::state",
        kind = "method",
        summary = "Validated inclusive range — rejects `min > max` instead of emitting an always-false `matches` fragment.",
        context = "Validated inclusive range — rejects `min > max` instead of emitting an always-false `matches` fragment. This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(min = "`min` provides the inclusive lower bound used to use validated inclusive range — rejects `min > max` instead of emitting an always-false `matches` fragment.", max = "`max` provides the inclusive upper bound used to use validated inclusive range — rejects `min > max` instead of emitting an always-false `matches` fragment."),
        returns = "The `sand :: command :: CommandResult < Condition >` value produced to use validated inclusive range — rejects `min > max` instead of emitting an always-false `matches` fragment.",
        example = "use sand::prelude::*;\n\nfn demonstrate<'a, T: 'static>(score_ref_value: sand::state::ScoreRef < 'a , T >, min: i32, max: i32)  {\n    let try_between = score_ref_value.try_between(min, max);\n}",
    )]
    pub fn try_between(self, min: i32, max: i32) -> sand_commands::CommandResult<Condition> {
        ScoreRange::Between(Some(min), Some(max)).validate()?;
        Ok(self.between(min, max))
    }

    /// `if score <sel> <obj> matches <lo>..<hi>` — within an inclusive range.
    ///
    /// Accepts any `RangeBounds<i32>`: `1..=100`, `0..`, `..100`, etc.
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreRef::matches",
        aliases = ["sand::prelude::ScoreRef::matches"],
        module = "sand::state",
        kind = "method",
        summary = "`if score <sel> <obj> matches <lo>..<hi>` — within an inclusive range.",
        context = "`if score <sel> <obj> matches <lo>..<hi>` — within an inclusive range. Accepts any `RangeBounds<i32>`: `1..=100`, `0..`, `..100`, etc.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(range = "`range` provides the accepted numeric range used to emit the documented `if score <sel> <obj> matches <lo>..<hi>` — within an inclusive range form."),
        returns = "The `Condition` value produced to emit the documented `if score <sel> <obj> matches <lo>..<hi>` — within an inclusive range form.",
        example = "use sand::prelude::*;\n\nfn demonstrate<'a, T: 'static>(score_ref_value: sand::state::ScoreRef < 'a , T >, range: impl std::ops::RangeBounds < i32 >)  {\n    let matches = score_ref_value.matches(range);\n}",
    )]
    pub fn matches(self, range: impl RangeBounds<i32>) -> Condition {
        use std::ops::Bound;
        let lo = match range.start_bound() {
            Bound::Included(&n) => Some(n),
            Bound::Excluded(&n) => Some(n + 1),
            Bound::Unbounded => None,
        };
        let hi = match range.end_bound() {
            Bound::Included(&n) => Some(n),
            Bound::Excluded(&n) => Some(n - 1),
            Bound::Unbounded => None,
        };
        let objective = self.obj();
        Condition::score(self.selector, objective, ScoreRange::Between(lo, hi))
    }

    /// Validated counterpart to [`ScoreRef::matches`] — rejects a range whose
    /// resolved bounds have `lo > hi` instead of emitting an always-false
    /// `matches` fragment.
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreRef::try_matches",
        aliases = ["sand::prelude::ScoreRef::try_matches"],
        module = "sand::state",
        kind = "method",
        summary = "Validated counterpart to [`ScoreRef::matches`] — rejects a range whose resolved bounds have `lo > hi` instead of emitting an always-false `matches` fragment.",
        context = "Validated counterpart to [`ScoreRef::matches`] — rejects a range whose resolved bounds have `lo > hi` instead of emitting an always-false `matches` fragment. This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(range = "`range` provides the accepted numeric range used to use validated counterpart to [`ScoreRef::matches`] — rejects a range whose resolved bounds have `lo > hi` instead of emitting an always-false `matches` fragment."),
        returns = "The `sand :: command :: CommandResult < Condition >` value produced to use validated counterpart to [`ScoreRef::matches`] — rejects a range whose resolved bounds have `lo > hi` instead of emitting an always-false `matches` fragment.",
        example = "use sand::prelude::*;\n\nfn demonstrate<'a, T: 'static>(score_ref_value: sand::state::ScoreRef < 'a , T >, range: impl std::ops::RangeBounds < i32 >)  {\n    let try_matches = score_ref_value.try_matches(range);\n}",
    )]
    pub fn try_matches(
        self,
        range: impl RangeBounds<i32>,
    ) -> sand_commands::CommandResult<Condition> {
        use std::ops::Bound;
        let lo = match range.start_bound() {
            Bound::Included(&n) => Some(n),
            Bound::Excluded(&n) => Some(n + 1),
            Bound::Unbounded => None,
        };
        let hi = match range.end_bound() {
            Bound::Included(&n) => Some(n),
            Bound::Excluded(&n) => Some(n - 1),
            Bound::Unbounded => None,
        };
        ScoreRange::Between(lo, hi).validate()?;
        let objective = self.obj();
        Ok(Condition::score(
            self.selector,
            objective,
            ScoreRange::Between(lo, hi),
        ))
    }
}

impl<'a, T> From<ScoreRef<'a, T>> for ScoreOperand {
    fn from(value: ScoreRef<'a, T>) -> Self {
        value.operand()
    }
}

#[sand_macros::api(
    registry = sand_api_contract,
    path = "sand::state::ScoreExpr",
    module = "sand::state",
    summary = "A compiler-managed sequence of vanilla scoreboard operations.",
    context = "A compiler-managed sequence of vanilla scoreboard operations. This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
    minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
    use_when = ["Working with typed gameplay state or composing state transitions"],
    avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
    example = "use sand::state::ScoreExpr;",
)]
/// A compiler-managed sequence of vanilla scoreboard operations.
pub struct ScoreExpr<T = i32> {
    base: ScoreOperand,
    steps: Vec<(ScoreOperation, ScoreOperand)>,
    _marker: PhantomData<T>,
}

impl<T> ScoreExpr<T> {
    fn operation<O: Into<ScoreOperand>>(mut self, op: ScoreOperation, other: O) -> Self {
        self.steps.push((op, other.into()));
        self
    }

    /// Adds another score operand to this expression.
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreExpr::plus",
        module = "sand::state",
        kind = "method",
        summary = "Adds another score operand to this expression.",
        context = "Adds another score operand to this expression. This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(other = "`other` supplies the other value used to add another score operand to this expression."),
        returns = "The `ScoreExpr` value with the documented change applied to add another score operand to this expression.",
        example = "use sand::prelude::*;\n\nfn demonstrate<T: 'static, O : Into < sand::state::ScoreOperand > + 'static>(score_expr_value: sand::state::ScoreExpr < T >, other: O)  {\n    let updated_score_expr = score_expr_value.plus::<O>(other);\n}",
    )]
    pub fn plus<O: Into<ScoreOperand>>(self, other: O) -> Self {
        self.operation(ScoreOperation::Add, other)
    }
    /// Subtracts another score operand from this expression.
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreExpr::minus",
        module = "sand::state",
        kind = "method",
        summary = "Subtracts another score operand from this expression.",
        context = "Subtracts another score operand from this expression. This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(other = "`other` supplies the other value used to subtract another score operand from this expression."),
        returns = "The `ScoreExpr` value with the documented change applied to subtract another score operand from this expression.",
        example = "use sand::prelude::*;\n\nfn demonstrate<T: 'static, O : Into < sand::state::ScoreOperand > + 'static>(score_expr_value: sand::state::ScoreExpr < T >, other: O)  {\n    let updated_score_expr = score_expr_value.minus::<O>(other);\n}",
    )]
    pub fn minus<O: Into<ScoreOperand>>(self, other: O) -> Self {
        self.operation(ScoreOperation::Sub, other)
    }
    /// Multiplies this expression by another score operand.
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreExpr::mul",
        module = "sand::state",
        kind = "method",
        summary = "Multiplies this expression by another score operand.",
        context = "Multiplies this expression by another score operand. This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(other = "`other` supplies the other value used to multiplie this expression by another score operand."),
        returns = "The `ScoreExpr` value with the documented change applied to multiplie this expression by another score operand.",
        example = "use sand::prelude::*;\n\nfn demonstrate<T: 'static, O : Into < sand::state::ScoreOperand > + 'static>(score_expr_value: sand::state::ScoreExpr < T >, other: O)  {\n    let updated_score_expr = score_expr_value.mul::<O>(other);\n}",
    )]
    #[allow(clippy::should_implement_trait)]
    pub fn mul<O: Into<ScoreOperand>>(self, other: O) -> Self {
        self.operation(ScoreOperation::Mul, other)
    }
    /// Divides this expression by another score operand using scoreboard arithmetic.
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreExpr::div",
        module = "sand::state",
        kind = "method",
        summary = "Divides this expression by another score operand using scoreboard arithmetic.",
        context = "Divides this expression by another score operand using scoreboard arithmetic. This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(other = "`other` supplies the other value used to divide this expression by another score operand using scoreboard arithmetic."),
        returns = "The `ScoreExpr` value with the documented change applied to divide this expression by another score operand using scoreboard arithmetic.",
        example = "use sand::prelude::*;\n\nfn demonstrate<T: 'static, O : Into < sand::state::ScoreOperand > + 'static>(score_expr_value: sand::state::ScoreExpr < T >, other: O)  {\n    let updated_score_expr = score_expr_value.div::<O>(other);\n}",
    )]
    #[allow(clippy::should_implement_trait)]
    pub fn div<O: Into<ScoreOperand>>(self, other: O) -> Self {
        self.operation(ScoreOperation::Div, other)
    }
    /// Applies scoreboard remainder arithmetic with another score operand.
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreExpr::modulo",
        module = "sand::state",
        kind = "method",
        summary = "Applies scoreboard remainder arithmetic with another score operand.",
        context = "Applies scoreboard remainder arithmetic with another score operand. This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(other = "`other` supplies the other value used to apply scoreboard remainder arithmetic with another score operand."),
        returns = "The `ScoreExpr` value with the documented change applied to apply scoreboard remainder arithmetic with another score operand.",
        example = "use sand::prelude::*;\n\nfn demonstrate<T: 'static, O : Into < sand::state::ScoreOperand > + 'static>(score_expr_value: sand::state::ScoreExpr < T >, other: O)  {\n    let updated_score_expr = score_expr_value.modulo::<O>(other);\n}",
    )]
    pub fn modulo<O: Into<ScoreOperand>>(self, other: O) -> Self {
        self.operation(ScoreOperation::Mod, other)
    }
    /// Clamps this expression to the lesser of itself and another score operand.
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreExpr::min",
        module = "sand::state",
        kind = "method",
        summary = "Clamps this expression to the lesser of itself and another score operand.",
        context = "Clamps this expression to the lesser of itself and another score operand. This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(other = "`other` supplies the other value used to clamp this expression to the lesser of itself and another score operand."),
        returns = "The `ScoreExpr` value with the documented change applied to clamp this expression to the lesser of itself and another score operand.",
        example = "use sand::prelude::*;\n\nfn demonstrate<T: 'static, O : Into < sand::state::ScoreOperand > + 'static>(score_expr_value: sand::state::ScoreExpr < T >, other: O)  {\n    let updated_score_expr = score_expr_value.min::<O>(other);\n}",
    )]
    pub fn min<O: Into<ScoreOperand>>(self, other: O) -> Self {
        self.operation(ScoreOperation::Min, other)
    }
    /// Clamps this expression to the greater of itself and another score operand.
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreExpr::max",
        module = "sand::state",
        kind = "method",
        summary = "Clamps this expression to the greater of itself and another score operand.",
        context = "Clamps this expression to the greater of itself and another score operand. This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(other = "`other` supplies the other value used to clamp this expression to the greater of itself and another score operand."),
        returns = "The `ScoreExpr` value with the documented change applied to clamp this expression to the greater of itself and another score operand.",
        example = "use sand::prelude::*;\n\nfn demonstrate<T: 'static, O : Into < sand::state::ScoreOperand > + 'static>(score_expr_value: sand::state::ScoreExpr < T >, other: O)  {\n    let updated_score_expr = score_expr_value.max::<O>(other);\n}",
    )]
    pub fn max<O: Into<ScoreOperand>>(self, other: O) -> Self {
        self.operation(ScoreOperation::Max, other)
    }

    fn lowered(self, condition: Condition) -> Conditional {
        request_expression_temp();
        let temp = ScoreOperand {
            selector: self.base.selector.clone(),
            objective: SCORE_EXPRESSION_TEMP_OBJECTIVE.to_string(),
        };
        let mut setup = vec![format!(
            "scoreboard players operation {} {} = {} {}",
            temp.selector, temp.objective, self.base.selector, self.base.objective
        )];
        setup.extend(self.steps.into_iter().map(|(op, right)| {
            format!(
                "scoreboard players operation {} {} {} {} {}",
                temp.selector,
                temp.objective,
                op.as_str(),
                right.selector,
                right.objective
            )
        }));
        Conditional::with_setup(setup, condition)
    }

    fn temp(&self) -> ScoreOperand {
        ScoreOperand {
            selector: self.base.selector.clone(),
            objective: SCORE_EXPRESSION_TEMP_OBJECTIVE.to_string(),
        }
    }

    /// Builds a condition requiring this expression to equal an integer.
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreExpr::eq",
        module = "sand::state",
        kind = "method",
        summary = "Builds a condition requiring this expression to equal an integer.",
        context = "Builds a condition requiring this expression to equal an integer. This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(n = "`n` supplies the n value used to build a condition requiring this expression to equal an integer."),
        returns = "The `Conditional` value produced to build a condition requiring this expression to equal an integer.",
        example = "use sand::prelude::*;\n\nfn demonstrate<T: 'static>(score_expr_value: sand::state::ScoreExpr < T >, n: i32)  {\n    let eq = score_expr_value.eq(n);\n}",
    )]
    pub fn eq(self, n: i32) -> Conditional {
        let temp = self.temp();
        self.lowered(Condition::score(
            temp.selector,
            temp.objective,
            ScoreRange::Eq(n),
        ))
    }
    /// Builds a condition requiring this expression to exceed an integer.
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreExpr::gt",
        module = "sand::state",
        kind = "method",
        summary = "Builds a condition requiring this expression to exceed an integer.",
        context = "Builds a condition requiring this expression to exceed an integer. This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(n = "`n` supplies the n value used to build a condition requiring this expression to exceed an integer."),
        returns = "The `Conditional` value produced to build a condition requiring this expression to exceed an integer.",
        example = "use sand::prelude::*;\n\nfn demonstrate<T: 'static>(score_expr_value: sand::state::ScoreExpr < T >, n: i32)  {\n    let gt = score_expr_value.gt(n);\n}",
    )]
    pub fn gt(self, n: i32) -> Conditional {
        let temp = self.temp();
        self.lowered(Condition::score(
            temp.selector,
            temp.objective,
            ScoreRange::Gt(n),
        ))
    }
    /// Builds a condition requiring this expression to be at least an integer.
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreExpr::gte",
        module = "sand::state",
        kind = "method",
        summary = "Builds a condition requiring this expression to be at least an integer.",
        context = "Builds a condition requiring this expression to be at least an integer. This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(n = "`n` supplies the n value used to build a condition requiring this expression to be at least an integer."),
        returns = "The `Conditional` value produced to build a condition requiring this expression to be at least an integer.",
        example = "use sand::prelude::*;\n\nfn demonstrate<T: 'static>(score_expr_value: sand::state::ScoreExpr < T >, n: i32)  {\n    let gte = score_expr_value.gte(n);\n}",
    )]
    pub fn gte(self, n: i32) -> Conditional {
        let temp = self.temp();
        self.lowered(Condition::score(
            temp.selector,
            temp.objective,
            ScoreRange::Gte(n),
        ))
    }
    /// Builds a condition requiring this expression to be below an integer.
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreExpr::lt",
        module = "sand::state",
        kind = "method",
        summary = "Builds a condition requiring this expression to be below an integer.",
        context = "Builds a condition requiring this expression to be below an integer. This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(n = "`n` supplies the n value used to build a condition requiring this expression to be below an integer."),
        returns = "The `Conditional` value produced to build a condition requiring this expression to be below an integer.",
        example = "use sand::prelude::*;\n\nfn demonstrate<T: 'static>(score_expr_value: sand::state::ScoreExpr < T >, n: i32)  {\n    let lt = score_expr_value.lt(n);\n}",
    )]
    pub fn lt(self, n: i32) -> Conditional {
        let temp = self.temp();
        self.lowered(Condition::score(
            temp.selector,
            temp.objective,
            ScoreRange::Lt(n),
        ))
    }
    /// Builds a condition requiring this expression to be at most an integer.
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreExpr::lte",
        module = "sand::state",
        kind = "method",
        summary = "Builds a condition requiring this expression to be at most an integer.",
        context = "Builds a condition requiring this expression to be at most an integer. This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(n = "`n` supplies the n value used to build a condition requiring this expression to be at most an integer."),
        returns = "The `Conditional` value produced to build a condition requiring this expression to be at most an integer.",
        example = "use sand::prelude::*;\n\nfn demonstrate<T: 'static>(score_expr_value: sand::state::ScoreExpr < T >, n: i32)  {\n    let lte = score_expr_value.lte(n);\n}",
    )]
    pub fn lte(self, n: i32) -> Conditional {
        let temp = self.temp();
        self.lowered(Condition::score(
            temp.selector,
            temp.objective,
            ScoreRange::Lte(n),
        ))
    }
    /// Builds a condition requiring this expression to fall within a score range.
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreExpr::matches",
        module = "sand::state",
        kind = "method",
        summary = "Builds a condition requiring this expression to fall within a score range.",
        context = "Builds a condition requiring this expression to fall within a score range. This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(range = "`range` provides the accepted numeric range used to build a condition requiring this expression to fall within a score range."),
        returns = "The `Conditional` value produced to build a condition requiring this expression to fall within a score range.",
        example = "use sand::prelude::*;\n\nfn demonstrate<T: 'static>(score_expr_value: sand::state::ScoreExpr < T >, range: impl std::ops::RangeBounds < i32 >)  {\n    let matches = score_expr_value.matches(range);\n}",
    )]
    pub fn matches(self, range: impl RangeBounds<i32>) -> Conditional {
        use std::ops::Bound;
        let lo = match range.start_bound() {
            Bound::Included(&n) => Some(n),
            Bound::Excluded(&n) => Some(n + 1),
            Bound::Unbounded => None,
        };
        let hi = match range.end_bound() {
            Bound::Included(&n) => Some(n),
            Bound::Excluded(&n) => Some(n - 1),
            Bound::Unbounded => None,
        };
        let temp = self.temp();
        self.lowered(Condition::score(
            temp.selector,
            temp.objective,
            ScoreRange::Between(lo, hi),
        ))
    }
    /// Builds a condition requiring this expression to equal another score operand.
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreExpr::eq_score",
        module = "sand::state",
        kind = "method",
        summary = "Builds a condition requiring this expression to equal another score operand.",
        context = "Builds a condition requiring this expression to equal another score operand. This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(other = "`other` supplies the other value used to build a condition requiring this expression to equal another score operand."),
        returns = "The `Conditional` value produced to build a condition requiring this expression to equal another score operand.",
        example = "use sand::prelude::*;\n\nfn demonstrate<T: 'static, O : Into < sand::state::ScoreOperand > + 'static>(score_expr_value: sand::state::ScoreExpr < T >, other: O)  {\n    let eq_score = score_expr_value.eq_score::<O>(other);\n}",
    )]
    pub fn eq_score<O: Into<ScoreOperand>>(self, other: O) -> Conditional {
        let left = self.temp();
        self.lowered(Condition::score_compare(
            left,
            ScoreCompareOp::Eq,
            other.into(),
        ))
    }
    /// Builds a condition requiring this expression to exceed another score operand.
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreExpr::gt_score",
        module = "sand::state",
        kind = "method",
        summary = "Builds a condition requiring this expression to exceed another score operand.",
        context = "Builds a condition requiring this expression to exceed another score operand. This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(other = "`other` supplies the other value used to build a condition requiring this expression to exceed another score operand."),
        returns = "The `Conditional` value produced to build a condition requiring this expression to exceed another score operand.",
        example = "use sand::prelude::*;\n\nfn demonstrate<T: 'static, O : Into < sand::state::ScoreOperand > + 'static>(score_expr_value: sand::state::ScoreExpr < T >, other: O)  {\n    let gt_score = score_expr_value.gt_score::<O>(other);\n}",
    )]
    pub fn gt_score<O: Into<ScoreOperand>>(self, other: O) -> Conditional {
        let left = self.temp();
        self.lowered(Condition::score_compare(
            left,
            ScoreCompareOp::Gt,
            other.into(),
        ))
    }
    /// Builds a condition requiring this expression to be at least another score operand.
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreExpr::gte_score",
        module = "sand::state",
        kind = "method",
        summary = "Builds a condition requiring this expression to be at least another score operand.",
        context = "Builds a condition requiring this expression to be at least another score operand. This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(other = "`other` supplies the other value used to build a condition requiring this expression to be at least another score operand."),
        returns = "The `Conditional` value produced to build a condition requiring this expression to be at least another score operand.",
        example = "use sand::prelude::*;\n\nfn demonstrate<T: 'static, O : Into < sand::state::ScoreOperand > + 'static>(score_expr_value: sand::state::ScoreExpr < T >, other: O)  {\n    let gte_score = score_expr_value.gte_score::<O>(other);\n}",
    )]
    pub fn gte_score<O: Into<ScoreOperand>>(self, other: O) -> Conditional {
        let left = self.temp();
        self.lowered(Condition::score_compare(
            left,
            ScoreCompareOp::Gte,
            other.into(),
        ))
    }
    /// Builds a condition requiring this expression to be below another score operand.
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreExpr::lt_score",
        module = "sand::state",
        kind = "method",
        summary = "Builds a condition requiring this expression to be below another score operand.",
        context = "Builds a condition requiring this expression to be below another score operand. This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(other = "`other` supplies the other value used to build a condition requiring this expression to be below another score operand."),
        returns = "The `Conditional` value produced to build a condition requiring this expression to be below another score operand.",
        example = "use sand::prelude::*;\n\nfn demonstrate<T: 'static, O : Into < sand::state::ScoreOperand > + 'static>(score_expr_value: sand::state::ScoreExpr < T >, other: O)  {\n    let lt_score = score_expr_value.lt_score::<O>(other);\n}",
    )]
    pub fn lt_score<O: Into<ScoreOperand>>(self, other: O) -> Conditional {
        let left = self.temp();
        self.lowered(Condition::score_compare(
            left,
            ScoreCompareOp::Lt,
            other.into(),
        ))
    }
    /// Builds a condition requiring this expression to be at most another score operand.
    #[sand_macros::api(
        registry = sand_api_contract,
        path = "sand::state::ScoreExpr::lte_score",
        module = "sand::state",
        kind = "method",
        summary = "Builds a condition requiring this expression to be at most another score operand.",
        context = "Builds a condition requiring this expression to be at most another score operand. This declaration provides the typed scoreboard or lifecycle primitives used directly and by #[derive(State)]; generated schema registration remains private.",
        minecraft = "Operations render validated scoreboard commands or conditions against the selected score holder, with lifecycle setup emitted at load when required.",
        use_when = ["Working with typed gameplay state or composing state transitions"],
        avoid_when = ["Manually reproducing metadata generated by #[derive(State)]"],
        params(other = "`other` supplies the other value used to build a condition requiring this expression to be at most another score operand."),
        returns = "The `Conditional` value produced to build a condition requiring this expression to be at most another score operand.",
        example = "use sand::prelude::*;\n\nfn demonstrate<T: 'static, O : Into < sand::state::ScoreOperand > + 'static>(score_expr_value: sand::state::ScoreExpr < T >, other: O)  {\n    let lte_score = score_expr_value.lte_score::<O>(other);\n}",
    )]
    pub fn lte_score<O: Into<ScoreOperand>>(self, other: O) -> Conditional {
        let left = self.temp();
        self.lowered(Condition::score_compare(
            left,
            ScoreCompareOp::Lte,
            other.into(),
        ))
    }
}

// ── Tests ─────────────────────────────────────────────────────────────────────

#[cfg(test)]
mod tests {
    use super::*;
    static MANA: ScoreVar<i32> = ScoreVar::new("mana");
    static LONG: ScoreVar<i32> = ScoreVar::new("this_is_a_very_long_name_that_exceeds_limit");

    #[test]
    fn short_name_unchanged() {
        assert_eq!(MANA.objective_name(), "mana");
    }

    #[test]
    fn long_name_hashed_stable() {
        let a = LONG.objective_name();
        let b = LONG.objective_name();
        assert_eq!(a, b, "hash must be deterministic");
        assert!(
            a.len() <= 16,
            "hashed name must be ≤16 chars, got {}",
            a.len()
        );
        assert!(a.starts_with('s'), "hashed name must start with 's'");
    }

    #[test]
    fn long_name_hashing_matches_sand_commands_canonical_algorithm() {
        // #146: sand-core's ScoreVar and sand-commands' Objective/ObjectiveName
        // must share one hashing algorithm, not two independently-maintained
        // ones. Prove ScoreVar's emitted name equals the canonical
        // ObjectiveName::logical output directly.
        static LOCAL: ScoreVar<i32> = ScoreVar::new("this_is_a_very_long_name_that_exceeds_limit");
        assert_eq!(
            LOCAL.objective_name(),
            sand_commands::ObjectiveName::logical("this_is_a_very_long_name_that_exceeds_limit")
                .as_str()
        );
    }

    #[test]
    fn short_invalid_name_is_hashed_rather_than_emitted_verbatim() {
        // Previously a bug: a short (<=16 char) name with a space would be
        // emitted verbatim (invalid `scoreboard objectives add` syntax).
        // objective_name now routes through ObjectiveName::logical, which
        // falls back to hashing instead of emitting an invalid direct token.
        static BAD: ScoreVar<i32> = ScoreVar::new("bad name");
        let emitted = BAD.objective_name();
        assert_ne!(emitted, "bad name");
        assert!(emitted.starts_with('s'));
        assert!(emitted.len() <= 16);
        assert!(!emitted.contains(' '));
    }

    #[test]
    fn define_cmd() {
        assert_eq!(MANA.define(), "scoreboard objectives add mana dummy");
    }

    #[test]
    fn set_cmd() {
        assert_eq!(MANA.set("@s", 100), "scoreboard players set @s mana 100");
    }

    #[test]
    fn add_cmd() {
        assert_eq!(MANA.add("@s", 5), "scoreboard players add @s mana 5");
    }

    #[test]
    fn remove_cmd() {
        assert_eq!(
            MANA.remove("@s", 10),
            "scoreboard players remove @s mana 10"
        );
    }

    #[test]
    fn reset_cmd() {
        assert_eq!(MANA.reset("@s"), "scoreboard players reset @s mana");
    }

    #[test]
    fn clamp_cmds() {
        let cmds = MANA.clamp("@s", 0, 100);
        assert_eq!(cmds.len(), 2);
        assert!(cmds[0].contains("matches ..-1"), "got: {}", cmds[0]);
        assert!(cmds[1].contains("matches 101.."), "got: {}", cmds[1]);
    }

    #[test]
    fn try_clamp_matches_clamp_for_valid_range() {
        assert_eq!(
            MANA.try_clamp("@s", 0, 100).unwrap(),
            MANA.clamp("@s", 0, 100)
        );
    }

    #[test]
    fn try_clamp_rejects_min_greater_than_max() {
        assert!(MANA.try_clamp("@s", 10, 5).is_err());
    }

    #[test]
    fn try_clamp_diagnostic_code_is_stable() {
        let err = MANA.try_clamp("@s", 10, 5).unwrap_err();
        assert_eq!(err.code, "SAND-SCORE-RANGE");
    }

    #[test]
    fn try_gt_rejects_i32_max() {
        assert!(MANA.of("@s").try_gt(i32::MAX).is_err());
        assert!(MANA.of("@s").try_gt(10).is_ok());
    }

    #[test]
    fn try_lt_rejects_i32_min() {
        assert!(MANA.of("@s").try_lt(i32::MIN).is_err());
        assert!(MANA.of("@s").try_lt(10).is_ok());
    }

    #[test]
    fn try_between_rejects_min_greater_than_max() {
        assert!(MANA.of("@s").try_between(10, 5).is_err());
        assert!(MANA.of("@s").try_between(5, 10).is_ok());
    }

    #[test]
    fn try_matches_rejects_impossible_range() {
        #[allow(clippy::reversed_empty_ranges)]
        let bad = MANA.of("@s").try_matches(10..=5);
        assert!(bad.is_err());
        assert!(MANA.of("@s").try_matches(1..=100).is_ok());
    }

    #[test]
    fn condition_gte() {
        let cond = MANA.of("@s").gte(25);
        match cond.kind() {
            ConditionKind::Score {
                selector,
                objective,
                range: ScoreRange::Gte(25),
            } => {
                assert_eq!(selector, "@s");
                assert_eq!(objective, "mana");
            }
            other => panic!("unexpected: {other:?}"),
        }
    }

    #[test]
    fn condition_lte() {
        let cond = MANA.of("@s").lte(100);
        match cond.kind() {
            ConditionKind::Score {
                range: ScoreRange::Lte(100),
                ..
            } => {}
            other => panic!("unexpected: {other:?}"),
        }
    }

    #[test]
    fn condition_ne_wraps_not() {
        let cond = MANA.of("@s").ne(0);
        assert!(matches!(cond.kind(), ConditionKind::Not(_)));
    }

    #[test]
    fn condition_matches_range() {
        let cond = MANA.of("@s").matches(1..=100);
        match cond.kind() {
            ConditionKind::Score {
                range: ScoreRange::Between(Some(1), Some(100)),
                ..
            } => {}
            other => panic!("unexpected: {other:?}"),
        }
    }

    #[test]
    fn condition_between() {
        let cond = MANA.of("@s").between(10, 100);
        match cond.kind() {
            ConditionKind::Score {
                range: ScoreRange::Between(Some(10), Some(100)),
                ..
            } => {}
            other => panic!("unexpected: {other:?}"),
        }
    }

    #[test]
    fn init_cmd_uses_unless() {
        let cmd = MANA.init("@s", 100);
        assert!(
            cmd.contains("unless score @s mana matches -2147483648.."),
            "got: {cmd}"
        );
        assert!(cmd.contains("set @s mana 100"), "got: {cmd}");
    }

    #[test]
    fn copy_within_cmd() {
        let cmd = MANA.copy_within("@s", "@p");
        assert_eq!(cmd, "scoreboard players operation @p mana = @s mana");
    }

    #[test]
    fn copy_to_cmd() {
        static OTHER: ScoreVar<i32> = ScoreVar::new("other");
        let cmd = MANA.copy_to("@s", &OTHER, "@p");
        assert_eq!(cmd, "scoreboard players operation @p other = @s mana");
    }

    #[test]
    fn copy_from_cmd() {
        static SRC: ScoreVar<i32> = ScoreVar::new("src");
        let cmd = MANA.copy_from("@s", &SRC, "@p");
        assert_eq!(cmd, "scoreboard players operation @s mana = @p src");
    }

    #[test]
    fn min_op_cmd() {
        static CAP: ScoreVar<i32> = ScoreVar::new("cap");
        let cmd = MANA.min_op("@s", &CAP, "@s");
        assert_eq!(cmd, "scoreboard players operation @s mana < @s cap");
    }

    #[test]
    fn max_op_cmd() {
        static FLOOR: ScoreVar<i32> = ScoreVar::new("floor");
        let cmd = MANA.max_op("@s", &FLOOR, "@s");
        assert_eq!(cmd, "scoreboard players operation @s mana > @s floor");
    }

    #[test]
    fn is_zero_condition() {
        let cond = MANA.is_zero("@s");
        assert!(matches!(
            cond.kind(),
            ConditionKind::Score {
                range: ScoreRange::Eq(0),
                ..
            }
        ));
    }

    #[test]
    fn is_nonzero_condition() {
        let cond = MANA.is_nonzero("@s");
        assert!(matches!(cond.kind(), ConditionKind::Not(_)));
    }

    #[test]
    fn positive_condition() {
        let cond = MANA.positive("@s");
        assert!(matches!(
            cond.kind(),
            ConditionKind::Score {
                range: ScoreRange::Gt(0),
                ..
            }
        ));
    }

    #[test]
    fn negative_condition() {
        let cond = MANA.negative("@s");
        assert!(matches!(
            cond.kind(),
            ConditionKind::Score {
                range: ScoreRange::Lt(0),
                ..
            }
        ));
    }

    #[test]
    fn scoreref_is_zero() {
        let cond = MANA.of("@s").is_zero();
        assert!(matches!(
            cond.kind(),
            ConditionKind::Score {
                range: ScoreRange::Eq(0),
                ..
            }
        ));
    }

    #[test]
    fn scoreref_positive() {
        let cond = MANA.of("@s").positive();
        assert!(matches!(
            cond.kind(),
            ConditionKind::Score {
                range: ScoreRange::Gt(0),
                ..
            }
        ));
    }

    #[test]
    fn condition_outside_wraps_not() {
        let cond = MANA.of("@s").outside(10, 100);
        assert!(matches!(cond.kind(), ConditionKind::Not(_)));
    }

    #[test]
    fn condition_matches_open_end() {
        let cond = MANA.of("@s").matches(25..);
        match cond.kind() {
            ConditionKind::Score {
                range: ScoreRange::Between(Some(25), None),
                ..
            } => {}
            other => panic!("unexpected: {other:?}"),
        }
    }

    #[test]
    fn score_comparisons_render_all_vanilla_operators() {
        static COST: ScoreVar<i32> = ScoreVar::new("cost");
        let cases = [
            (MANA.of("@s").eq_score(COST.of("@s")), "="),
            (MANA.of("@s").gt_score(COST.of("@s")), ">"),
            (MANA.of("@s").gte_score(COST.of("@p")), ">="),
            (MANA.of("@s").lt_score(COST.of("@p")), "<"),
            (MANA.of("@s").lte_score(COST.of("@p")), "<="),
        ];
        for (condition, operator) in cases {
            assert_eq!(
                condition.execute_commands(false, "say ok")[0],
                format!(
                    "execute if score @s mana {operator} {} cost run say ok",
                    if operator == "=" || operator == ">" {
                        "@s"
                    } else {
                        "@p"
                    }
                )
            );
        }
    }

    #[test]
    fn score_operations_render_all_symbols() {
        static OTHER: ScoreVar<i32> = ScoreVar::new("other");
        let actual = [
            MANA.of("@s").assign(OTHER.of("@p")),
            MANA.of("@s").add_score(OTHER.of("@p")),
            MANA.of("@s").sub_score(OTHER.of("@p")),
            MANA.of("@s").mul_score(OTHER.of("@p")),
            MANA.of("@s").div_score(OTHER.of("@p")),
            MANA.of("@s").mod_score(OTHER.of("@p")),
            MANA.of("@s").min_score(OTHER.of("@p")),
            MANA.of("@s").max_score(OTHER.of("@p")),
            MANA.of("@s").swap(OTHER.of("@p")),
        ];
        for (command, operator) in actual
            .iter()
            .zip(["=", "+=", "-=", "*=", "/=", "%=", "<", ">", "><"])
        {
            assert_eq!(
                command,
                &format!("scoreboard players operation @s mana {operator} @p other")
            );
        }
    }

    #[test]
    fn constants_register_setup_and_support_negative_values() {
        let constant = ScoreConst::<i32>::new("negative scale", -2);
        let command = MANA.of("@s").mul_score(constant.ref_());
        assert!(command.contains("#sand_negative"));
        let setup = drain_internal_score_setup();
        assert!(
            setup
                .iter()
                .any(|line| line == "scoreboard objectives add sand_consts dummy")
        );
        assert!(setup.iter().any(|line| line.ends_with(" sand_consts -2")));
    }

    #[test]
    fn set_percent_generates_integer_math_pipeline() {
        static DAMAGE: ScoreVar<i32> = ScoreVar::new("damage");
        let cmds = MANA.of("@s").set_percent(DAMAGE.of("@s"), 150);

        assert_eq!(cmds.len(), 3);
        assert_eq!(cmds[0], "scoreboard players operation @s mana = @s damage");
        assert!(
            cmds[1].starts_with("scoreboard players operation @s mana *= #sand_score_percent_150_")
        );
        assert!(cmds[1].ends_with(" sand_consts"));
        assert!(
            cmds[2].starts_with(
                "scoreboard players operation @s mana /= #sand_score_percent_denominator_"
            ),
            "got: {}",
            cmds[2]
        );
        assert!(cmds[2].ends_with(" sand_consts"));
    }

    #[test]
    fn scale_percent_updates_target_in_place() {
        let cmds = MANA.of("@s").scale_percent(75);

        assert_eq!(cmds.len(), 2);
        assert!(
            cmds[0].starts_with("scoreboard players operation @s mana *= #sand_score_percent_75_")
        );
        assert!(cmds[0].ends_with(" sand_consts"));
        assert!(
            cmds[1].starts_with(
                "scoreboard players operation @s mana /= #sand_score_percent_denominator_"
            ),
            "got: {}",
            cmds[1]
        );
    }

    #[test]
    fn set_ratio_generates_current_scale_divide_pipeline() {
        static HEALTH: ScoreVar<i32> = ScoreVar::new("health");
        static MAX_HEALTH: ScoreVar<i32> = ScoreVar::new("max_health");

        let cmds = MANA
            .of("@s")
            .set_ratio(HEALTH.of("@s"), MAX_HEALTH.of("@s"), 100);

        assert_eq!(cmds.len(), 3);
        assert_eq!(cmds[0], "scoreboard players operation @s mana = @s health");
        assert!(
            cmds[1].starts_with(
                "scoreboard players operation @s mana *= #sand_score_ratio_scale_100_"
            )
        );
        assert!(cmds[1].ends_with(" sand_consts"));
        assert_eq!(
            cmds[2],
            "scoreboard players operation @s mana /= @s max_health"
        );
    }

    #[test]
    fn safe_divide_guards_zero_divisor() {
        static MAX_HEALTH: ScoreVar<i32> = ScoreVar::new("max_health");

        let cmds = MANA.of("@s").safe_divide(MAX_HEALTH.of("@s"), 0);

        assert_eq!(
            cmds,
            vec![
                "execute unless score @s max_health matches 0 run scoreboard players operation @s mana /= @s max_health",
                "execute if score @s max_health matches 0 run scoreboard players set @s mana 0",
            ]
        );
    }

    #[test]
    fn safe_divide_supports_same_target_and_divisor() {
        let cmds = MANA.of("@s").safe_divide(MANA.of("@s"), 42);

        assert_eq!(
            cmds,
            vec![
                "execute unless score @s mana matches 0 run scoreboard players operation @s mana /= @s mana",
                "execute if score @s mana matches 0 run scoreboard players set @s mana 42",
            ]
        );
    }

    #[test]
    fn clamp_score_uses_vanilla_min_max_operations() {
        static MIN_MANA: ScoreVar<i32> = ScoreVar::new("min_mana");
        static MAX_MANA: ScoreVar<i32> = ScoreVar::new("max_mana");

        let cmds = MANA
            .of("@s")
            .clamp_score(MIN_MANA.of("@s"), MAX_MANA.of("@s"));

        assert_eq!(
            cmds,
            vec![
                "scoreboard players operation @s mana > @s min_mana",
                "scoreboard players operation @s mana < @s max_mana",
            ]
        );
    }

    #[test]
    fn saturating_add_and_sub_emit_literal_clamps() {
        let add = MANA.of("@s").saturating_add(5, 0, 100);
        assert_eq!(
            add,
            vec![
                "scoreboard players add @s mana 5",
                "execute if score @s mana matches ..-1 run scoreboard players set @s mana 0",
                "execute if score @s mana matches 101.. run scoreboard players set @s mana 100",
            ]
        );

        let sub = MANA.of("@s").saturating_sub(20, 0, 100);
        assert_eq!(
            sub,
            vec![
                "scoreboard players remove @s mana 20",
                "execute if score @s mana matches ..-1 run scoreboard players set @s mana 0",
                "execute if score @s mana matches 101.. run scoreboard players set @s mana 100",
            ]
        );
    }

    // ── #146: typed ScoreHolder-validated API ─────────────────────────────

    use sand_commands::ScoreHolder;

    #[test]
    fn try_set_matches_infallible_set_for_valid_holder() {
        assert_eq!(
            MANA.try_set(ScoreHolder::self_(), 100).unwrap(),
            MANA.set("@s", 100)
        );
    }

    #[test]
    fn try_add_try_remove_try_reset_match_infallible_variants() {
        assert_eq!(
            MANA.try_add(ScoreHolder::self_(), 5).unwrap(),
            MANA.add("@s", 5)
        );
        assert_eq!(
            MANA.try_remove(ScoreHolder::self_(), 5).unwrap(),
            MANA.remove("@s", 5)
        );
        assert_eq!(
            MANA.try_reset(ScoreHolder::self_()).unwrap(),
            MANA.reset("@s")
        );
    }

    #[test]
    fn try_init_matches_infallible_init() {
        assert_eq!(
            MANA.try_init(ScoreHolder::self_(), 100).unwrap(),
            MANA.init("@s", 100)
        );
    }

    #[test]
    fn try_copy_within_matches_infallible_copy_within() {
        assert_eq!(
            MANA.try_copy_within(ScoreHolder::self_(), ScoreHolder::player("Notch"))
                .unwrap(),
            MANA.copy_within("@s", "Notch")
        );
    }

    #[test]
    fn try_of_matches_infallible_of() {
        let cond = MANA.try_of(ScoreHolder::self_()).unwrap().gte(25);
        assert_eq!(cond, MANA.of("@s").gte(25));
    }

    #[test]
    fn typed_holder_apis_reject_invalid_fake_player_holders() {
        // A fake-player holder with whitespace is not valid vanilla
        // score-holder syntax; the typed path must reject it rather than
        // silently emit malformed mcfunction output.
        let bad = ScoreHolder::fake("bad holder");
        assert!(MANA.try_set(bad.clone(), 1).is_err());
        assert!(MANA.try_add(bad.clone(), 1).is_err());
        assert!(MANA.try_remove(bad.clone(), 1).is_err());
        assert!(MANA.try_reset(bad.clone()).is_err());
        assert!(MANA.try_init(bad.clone(), 1).is_err());
        assert!(
            MANA.try_copy_within(bad.clone(), ScoreHolder::self_())
                .is_err()
        );
        assert!(
            MANA.try_copy_within(ScoreHolder::self_(), bad.clone())
                .is_err()
        );
        assert!(MANA.try_of(bad).is_err());
    }

    #[test]
    fn typed_holder_apis_accept_fake_players_and_wildcards() {
        assert!(MANA.try_set(ScoreHolder::fake("#total_kills"), 0).is_ok());
        assert!(MANA.try_reset(ScoreHolder::wildcard()).is_ok());
        assert!(MANA.try_set(ScoreHolder::player("Notch"), 1).is_ok());
    }

    #[test]
    fn typed_holder_apis_accept_entity_selectors() {
        use sand_commands::selector::Selector;
        assert!(
            MANA.try_set(ScoreHolder::entity(Selector::all_players()), 1)
                .is_ok()
        );
        assert!(
            MANA.try_add(ScoreHolder::entity(Selector::self_()), 1)
                .is_ok()
        );
    }

    #[test]
    fn try_of_rejects_wildcard_and_multi_entity_holders() {
        // `execute if/unless score <holder> ...` requires exactly one score
        // holder; unlike `set`/`add`/`remove`/`reset`, a wildcard or a
        // selector matching more than one entity must be rejected rather
        // than silently producing `execute if score * mana matches ...` /
        // `execute if score @a mana matches ...`, which Minecraft refuses to
        // parse.
        use sand_commands::selector::Selector;
        assert!(MANA.try_of(ScoreHolder::wildcard()).is_err());
        assert!(
            MANA.try_of(ScoreHolder::entity(Selector::all_players()))
                .is_err()
        );
        assert!(
            MANA.try_of(ScoreHolder::entity(Selector::all_entities()))
                .is_err()
        );
        // Single-holder cases remain accepted.
        assert!(MANA.try_of(ScoreHolder::self_()).is_ok());
        assert!(MANA.try_of(ScoreHolder::player("Notch")).is_ok());
    }

    #[test]
    fn try_copy_within_rejects_wildcard_or_multi_entity_source() {
        // `scoreboard players operation <dst> <obj> = <src> <obj>` allows a
        // multi-holder `dst` but requires a single-holder `src`.
        use sand_commands::selector::Selector;
        assert!(
            MANA.try_copy_within(ScoreHolder::wildcard(), ScoreHolder::self_())
                .is_err()
        );
        assert!(
            MANA.try_copy_within(
                ScoreHolder::entity(Selector::all_players()),
                ScoreHolder::self_()
            )
            .is_err()
        );
        // A multi-holder destination is fine — only the source must be single.
        assert!(
            MANA.try_copy_within(ScoreHolder::self_(), ScoreHolder::wildcard())
                .is_ok()
        );
        assert!(
            MANA.try_copy_within(
                ScoreHolder::self_(),
                ScoreHolder::entity(Selector::all_players())
            )
            .is_ok()
        );
    }
}
