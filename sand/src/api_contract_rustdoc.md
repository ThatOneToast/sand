# API Contract

This page is part of Sand's supported author API. Copy the canonical `sand::...`
path shown at the top of the page and run `sand api show <canonical-path>` for
the validated contract, exact source-derived signature, parameters, return
type, Minecraft behavior, availability, and aliases. For an associated item,
append its field, variant, constant, or method name to the displayed owner path.
